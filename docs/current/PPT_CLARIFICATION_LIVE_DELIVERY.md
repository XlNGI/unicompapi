# PPT 主题追问运行交付修复

2026-09-11。负责人截图原句：“我要生成一个ppt”。实际表现为直接生成文档内容，仍展示四步卡片，没有主题追问。

## 原因与实际交付

上轮提交 `f9b61cc` 位于 `feature/conversational-ppt-web-foundation`，未应用到主工作区。运行进程 PID 13452 于 00:35:36 启动，来自主工作区 Electron；主工作区此前缺少追问实现。独立分支通过测试不等于用户正在使用的应用已修复。

本轮将已验收提交的 src/tests 补丁合入主工作区文件，并加入截图原句回归；既有代码清理改动、原有交接资料删除保持。原差异备份位于 `.cache/ppt-live-fix/workspace-before.patch`，未推送或替用户提交清理工作。

现在请求“我要生成一个ppt”进入 `needs_clarification`，助手回复：“你想做什么主题的 PPT？可以告诉我用途，比如工作汇报、产品介绍或教学，也可以先添加参考资料。”此时不开始模型生成。追问保存在消息历史，支持刷新恢复；文档进度使用对话文字呈现。

主工作区重新生产构建完成。旧应用通过 CloseMainWindow 正常退出后重新启动，不强杀进程，不重发截图中的请求，不自动继续付费调用。新需求应在新建对话测试；已有错误任务保留原记录。

实际启动核对：新进程 PID 11276 来自主工作区，加载构建 `assets/index-DQ6jC11h.js`；窗口标题 UniComp、窗口已显示且 Responding=true，启动错误日志为空。首次以隐藏进程方式启动后，已显式显示该进程的 UniComp 窗口，避免用户面对仍不可见的更新。

## 验证

- 使用实际生产 `dist`、preload 和 `electron/main.js`，仅注入隔离合成项目位置，配置独立测试用户数据；真实点击发送按钮，经真实 IPC、意图服务、JSON 仓储完成。没有伪造助手回复或 workflow 结果。
- 验证原句得到主题追问、两条持久消息、没有旧步骤卡片，模型响应调用 0、网络请求 0。完整应用截图已查看。
- 主工作区完整 Vitest：207 文件、1527 通过、0 失败/跳过。
- Node/UI：355 通过、1 失败；失败为原有 handoff 缺失（`manifests/SHA256SUMS.txt`），不恢复用户删除，不声明全量门禁通过。
- 类型检查、全仓 lint、真实生产构建、平台审计与差异检查通过。Vite 保留既有大 chunk 提示。
- 验证脚本初次因加载 Electron 主模块时机和 listConversations 参数错误失败，修正测试脚本后通过；生产代码不依赖脚本修补。

证据：`.cache/ppt-live-fix/production-e2e-result.json`、`production-topic.png`、`production-e2e.log`、`vitest.log`、`node-ui.log`、`lint.log` 与启动日志。所有测试素材均为合成数据，无凭证读取或真实付费接口调用。macOS 未实机验证。

本次关闭的是截图中的“未追问且显示旧卡片”交付问题。原生联网、全部对话事件、逐页制作/预览/修改和恢复仍按总计划继续，未以此次局部修复宣称完成。
