# 视频缩略图分支合并交付

日期：2026-09-11。负责人在确认已删除文件不会恢复后，明确授权将 `fix/video-thumbnail-preview` 合并到 `develop`。本次属于独立维护，依据当前负责人决策、AGENTS.md、PLANS.md 顶部维护事实及实际源码验收；不恢复历史阶段计划。

## 合并范围与删除保护

- 目标基线：`97d924a523c7fba2545a97d0e5e2dc6368bdf97d`。
- 来源分支：`fb7eb69c3ab86235bb0bb4fb23ab8921ab49feb1`，包含 3 个独有提交。
- 共同祖先：`3efaca8d02a2d9956960653805a5a3d61844c763`。
- 从 develop 创建隔离维护工作树及 `feature/integrate-video-thumbnail-preview`，执行正常三方、非快进合并。唯一文本冲突为 PLANS.md 的两处追加记录，保留双方内容，develop 最新维护事实在前。
- develop 已删除的 64 个路径继续保持删除：62 个 handoff 资料文件，以及 `src/pages/creation/CreationModePage.tsx`、`src/ui/layout/PageShell.tsx`。来源分支没有修改这些路径；对合并索引、文件系统及最终提交树核对，恢复数为 0。
- 合入视频缩略图规格及缓存、拖动解码与时间线交互、作品库布局、公共选择器弹层、任务日期筛选。保留来源分支和集成分支，便于追溯。

## 必要集成修正

1. 常驻视频页的初始化 effect 原来用局部 `active` 遮蔽可见性 prop，导致隐藏页也读取项目和草稿。现在先检查页面可见性，异步取消单独使用 `cancelled`，隐藏后的迟到结果或失败不覆盖新工作区。
2. develop 后续新增的服务商搜索协议下拉框接入公共 Pickers，满足弹层所有使用点的一致约束；由现有选择器合同测试发现并验证。
3. 清除合并自动带回聊天页的无用 SelectPicker import；develop 已经移除该使用点。

新增 4 项组件行为回归：隐藏初挂不读 IPC、激活加载与往返保留草稿、隐藏后迟到 session/草稿不写入，以及旧请求失败不覆盖重新激活的状态。测试执行真实组件 effect/cleanup，并检查 IPC 调用和渲染属性；不代表浏览器或原生窗口视觉验收。

## 验证结果

| 验证 | 实际结果 |
| --- | --- |
| 全量 Vitest | 212 文件、1560/1560 通过，0 跳过；使用 `--maxWorkers=2 --minWorkers=1` |
| Node/UI | 359/360 通过，0 跳过；唯一失败是已删除 handoff 的 `manifests/SHA256SUMS.txt` 缺失 |
| 真实媒体 | 真实 FFmpeg 40 帧缩略条及比例测试通过，使用现有本地开发媒体工具，无真实外部调用 |
| 类型检查 | 应用、测试配置通过 |
| lint / 生产 build | 通过；Vite 保留既有大 chunk 提示 |
| 平台 / 恢复审计 | 通过，无违规项 |
| 生产 Electron preload / IPC | 6 项合成项目冒烟通过，网络请求 0、模型调用 0 |
| 原工作区保护 | 开始时 29 个已修改/未跟踪文件逐项 SHA-256 校验完全一致 |
| 删除保护 / diff 检查 | 64 路径保持缺失，差异检查通过 |

首轮默认并发 Vitest 为 1553 通过、3 个 Office 测试触发 5 秒超时；未修改超时或测试断言，降低并发后全量通过。首轮 Node/UI 还发现服务商选择器未接入公共包装，修复后仅剩既有 handoff 缺失。首轮 lint 发现聊天页无用 import，清理后通过。保留首轮结果，不把复验结果改写为首轮全绿。

不恢复删除资料、不跳过 handoff 校验来消除既有失败。此合并完成不等于全套历史交接资料校验通过、商店包交付或 macOS 实机验收。

## 交付边界与后续

主工作区保持 `feature/provider-chat-availability-fix`，此前服务商、联网感知、提示词分流等未提交修复未混入本次合并，也未被覆盖。主工作区正在运行的应用未重启。集成分支和 develop 的提交、远端一致性在实际合并推送后再次核对；不强推、不删除来源分支。

验收日志位于隔离工作树 `tmp/merge-*`，删除集合与主工作区原文件 Hash 证据位于主工作区 `tmp/video-thumbnail-merge-audit.json`、`tmp/video-thumbnail-workspace-before.json`。这些本地临时证据、依赖、媒体二进制和生成物均不进入提交。

下一步可在负责人安排下，对最新 develop 构建做原生导入、连续跨片段拖动、长 GOP/高分辨率素材及导出的人工复验；另行处理已经删除交接资料后历史校验的维护口径。
