# 基础编辑预览修复

## 2026-09-22 播放与时间线交互修复

按已审核方案完成基础编辑页的局部修复：播放按钮只显示播放/暂停两态；用户暂停、缓冲或切片切换时不会因迟到媒体事件把播放意图错误改写；自然播放结束锁定在总时长，下一次点击从 0 秒重播。播放头越过时间线视口右侧后按分页方式推进横向滚动，使播放头在新视口左侧重新出现。视频片段拖动幽灵和插入线渲染在视频主轨内部，按整个主轨的横坐标计算插入位置，文字轨和音乐轨不接收视频片段拖放。

验证：`pnpm typecheck`、`pnpm lint`、时间线 Vitest 18/18、编辑器预览 Electron 19/19、播放交互 Electron 8/8（含自然结束和首次点击从 0 秒重播）通过。UI 合同中与本次实现直接相关的断言已更新并通过；S6/S13 仍有既有布局合同与当前工作区基线不一致，未借本次播放修复改动布局。

2026-09-21，负责人已确认按方案实施。当前分支 `feature/basic-editing-mp4-export`；保留既有 MP4 导出等未提交改动。唯一写入者为当前任务。

## 范围与验收

只修基础编辑时间轴帧图、导出成功按钮排列和导出预览放大裁切。源码 owner 为 `VideoEditingPage.tsx` 与 `pages.css`，沿用现有媒体 IPC、采样、主题和按钮组件。布局参考用户四张诊断截图、当前编辑器及共享 VideoPreview 的完整等比预览。不得修改用户素材、真实草稿、凭证、编码合同或其他页面布局，不提交、推送或发布。

本任务为既有合同内的局部维护（D1）。缓存行为使用 T2：先在真实 React/Electron 与隔离媒体边界复现，再修改并验证；布局使用 T3：同一窗口、素材和操作比较实际尺寸与截图。资源边界保持现有并发、缓存上限和取消机制，不引入无限重试。图片句柄的过期不等于已经解码的图片立即消失，不能仅凭五分钟 TTL 认定现场触发原因。

## 阶段

1. 复现（完成）：隔离 profile、合成三段素材，记录帧加载、尺寸、按钮位置、放大裁切；不操作真实项目。
2. 图片帧（完成）：修正复现命中的加载/缓存生命周期和空槽遮挡；扩大滚动/缩放与取消回归在阶段 4 进行。
3. 布局与放大（完成）：导出按钮同行且不截字；横/竖/方形视频完整等比显示，位置、Esc 和滚动恢复正常。
4. 回归（完成）：播放状态、多窗口与深浅主题，定向测试、类型、lint、生产构建、Electron 证据完成；已更新本记录及 PLANS 索引。

每阶段完成先报告证据再继续；失败则停止依赖步骤并诊断。授权覆盖以上四阶段，无须重复确认。若需扩大到数据迁移、新依赖、发布或无关改动，则重新确认。Windows 隔离验证不代表用户真实项目或 macOS 验收。

## 证据

输出统一置于 `outputs/editing-preview-fix/`，不纳入 Git。Host 自动前置门禁未验证；本次局部纠正不新增 UI 设计产物。

阶段 1：`before/report.json` 记录真实 React/Electron 复现。三段合成 MP4 初次帧图加载成功，联络图不可用时 Canvas 回退成功；模拟句柄到期并改变视口后请求数仍为 3（失败），按钮 y=414/458（失败），竖屏展开高 2133.33 而视口高 785，方形展开高 1200（失败），横屏正常。真实草稿写入 0。历史截图具体触发条件仍不据此反推为已确认。

复现命令：清除 `ELECTRON_RUN_AS_NODE` 与 `NODE_OPTIONS` 后运行 `node_modules/.bin/electron.cmd scripts/verify-editing-preview-electron.cjs --before`。使用合成视频、真实组件及隔离 IPC，阻断 HTTP(S)。

阶段 2：`--frames-only` 的 `frames/report.json` 六项通过：三段首屏图、到期后重新获取（3→6 次）、图片错误回退 Canvas 且不重新循环请求、联络图不可用回退、无帧不绘制空槽、草稿写入 0。前端保留句柄到期时间，视口请求前淘汰即将到期地址；图片加载失败后该片段转 Canvas，直到来源改变或主动清除缓存，不无限自动修正。空槽不再覆盖底层真实封面或已有无图占位。

阶段 3：首轮完整 Electron 十项通过，按钮坐标均 y=458，宽142/86；三种比例放大后都位于可用视口内，当前时间1秒保留，Esc 后无展开层且 body 滚动恢复。产品改动只在导出结果作用域，未改共享播放器和导出格式。类型检查、lint、编辑器 Node/UI 34/34 已通过。

## 最终验证与交付边界

实现与隔离验收完成，真实项目人工验收待进行。阶段 4 最终 `after/report.json` 为 19/19：三段真实解码帧、过期后重取、图片错误回退且无循环请求、连续缩放/滚动、按钮同行与文字完整、横/竖/方形展开适配、暂停位置与播放状态保留、Esc/滚动恢复、1079/1295/1440 宽度深浅主题，以及两个按钮原导航目的地。最后一轮通过真实 ThemeProvider 同时切换 RSuite 与项目主题；不把此前仅切 CSS 主题的观察作为最终主题验收。无真实项目读写或网络调用，隔离草稿写入 0。截图 `after/light-result.png` 与 `after/expanded-0.png` 展示按钮/时间轴和完整竖屏画面。

相关 Vitest 37/37（timeline-view、scrub-decoder、visibility-behavior、preview、media-controller），包含真实 FFmpeg 40 帧图生成；Node/UI 编辑器五文件共 34/34；`pnpm typecheck`、`pnpm lint`、`pnpm build`、平台审计、diff 检查通过。构建仍提示既有 Vite CJS 与大 chunk 警告。平台审计首轮发现验收脚本直接读取运行平台，已改为复用 `media-engine-common.mjs`，最终 violations=[]。最终源文件与构建 Hash 记录于 `outputs/editing-preview-fix/source-hashes.json`。

没有新增依赖、改变媒体 IPC/授权/导出编码、清理真实缓存或修改用户素材。当前分支原有 MP4 改动保留，未提交、推送、合并。未重启已有应用进程；未验证 macOS、打包安装、用户截图中实际项目的触发条件或全应用流程。隔离验证覆盖真实页面和解码，IPC/句柄失效由夹具控制，不宣称等同于真实项目端到端验收。

下一步：重启当前分支构建，在原项目进入基础编辑，确认时间轴帧图、两个按钮同行和竖屏完整放大；停留超过五分钟后滚动/缩放再次核对。若实际素材仍加载失败，应读取其具体失败边界继续排查，不能用本次合成素材通过来判定素材本身正常。
