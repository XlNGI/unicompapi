# 视频缩略图分支合并前删除文件核对

日期：2026-09-11。负责人要求检查 `fix/video-thumbnail-preview`，确保此前删除的文件不会重新进入 `develop`。本次仅获取远端分支、检查 Git 增量和模拟三方合并，未检出旧分支、未合并到 develop、未修改视频业务代码或用户原有工作区改动。

## 结论

以本次获取的分支版本进行正常三方合并，已删除文件恢复数为 **0**。不需要为保留删除状态而重新恢复或拷贝旧分支文件。

| 核对项 | 结果 |
| --- | --- |
| 本地及远端 develop | `97d924a523c7fba2545a97d0e5e2dc6368bdf97d` |
| origin/fix/video-thumbnail-preview | `fb7eb69c3ab86235bb0bb4fb23ab8921ab49feb1` |
| 共同祖先 | `3efaca8d02a2d9956960653805a5a3d61844c763` |
| develop 相对祖先删除 | 64 文件：62 个 handoff 资料文件、2 个旧源码文件 |
| 这 64 个文件在旧分支中仍存在 | 64 |
| 旧分支对这些文件作了修改 | 0 |
| 三方合并树中恢复这些文件 | 0 |
| 合并冲突 | 仅 `PLANS.md` 内容冲突 |

两个被删除源码文件为 `src/pages/creation/CreationModePage.tsx`、`src/ui/layout/PageShell.tsx`。删除已由 `1168441` 提交并进入 develop，因此不是仅存在于未提交工作区的删除；旧分支没有修改它们，三方合并会保留删除。

直接比较两分支完整文件树会看到旧分支仍有这些文件，这不等于它们会作为新增文件被合入。应检查共同祖先以来的增量和实际合并树。切勿通过整目录复制、从旧分支覆盖文件树或在冲突时整体采用旧版本来集成。

## 分支实际范围

该分支独有 3 个提交：

- `8a7b169`：作品库详情布局。
- `795dc80`：作品库展开预览和窄窗口滚动。
- `fb7eb69`：视频时间轴/预览，同时包含通用选择器及任务筛选修改。

相对共同祖先，共 31 个变更文件，24 修改、7 新增。新增均为当前源码/测试：`Pickers.tsx`、`video-scrub-cache.ts`、`task-date-range.ts`、`video-editor-thumbnail-spec.ts` 与三个测试文件；没有新增旧交接资料或已删除源码。

这不是仅视频缩略图修复：还包含作品库、跨页面下拉框弹层、任务日期筛选和视频页常驻挂载。若后续整分支集成，应保留这些依赖的一致性，并审查该范围。`PLANS.md` 应保留 develop 顶部最新维护事实，将仍相关的分支说明单独整理，不能整文件采用旧分支版本。

另有一项静态风险：视频页改为常驻后，载入 effect 内局部变量 `active` 遮蔽页面可见性 prop `active`，其早退判断不能阻止隐藏页初始化。后续业务验收应覆盖未进入视频页、离开视频页、切换项目时的草稿读取和预览资源生命周期。此项不影响本次“删除文件不会恢复”的 Git 结论，也未在本轮运行功能测试。

## 验证与边界

执行 `git fetch --no-tags origin develop fix/video-thumbnail-preview` 后核对精确提交；采用 `git merge-tree --write-tree` 模拟，无工作区 checkout/merge。比较共同祖先到 develop 的删除集合与模拟树实际文件集合，交集为空；没有内容修改/删除冲突。

机器可读证据与全部 64 个保护路径：`tmp/video-thumbnail-merge-audit.json`。其中模拟树 `61f9824f28b8f960c2a7d13c06a758ef5fa82d4e` 含尚未解决的 PLANS 冲突，仅用于文件集合核对，不能作为可发布构建。主工作区仍为 `feature/provider-chat-availability-fix`，此前未提交修复保持原样；没有提交、推送、实际合并或重新构建。

下一步若继续集成：在隔离的 develop 派生分支做三方合并，处理 PLANS 冲突、验收分支功能，并在最终提交树上再次检查上述 64 路径全部缺失。分支头改变时重新核对，本报告不替未来提交作保证。
