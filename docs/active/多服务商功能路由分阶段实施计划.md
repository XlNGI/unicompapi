# 多服务商功能路由分阶段实施计划

日期：2026-07-31

状态：M0—M4 与 M5 第一、二支已完成并合并；M5 第三支已实现并通过自验收，等待记录提交、推送与合并

计划基线：`develop@01142f7`；当前执行基线：`develop@bc91fda`

权威方案：`docs/active/阶段9-UI与多服务商功能路由最终方案.md`

## 一、计划结论

本项属于较大规模的后台契约、协议适配器、路由快照、历史迁移和 UI 接线工程，不能一次性实现，也不应以一个大 PR 收口。

实施采用“一支一验收、一支一合并”的节奏。每支合并后，下一支必须重新从最新 `develop` 创建。项目负责人于 2026-08-03 最新授权 Codex 按计划连续托管完成阶段 10 之前的 M2—M6，不再要求每支之间手工确认；该授权不包含阶段 10、真实服务商联网、真实凭证读取/验证或收费调用，每支仍须保留独立分支、完整证据和停止条件。

## 二、启动闸门

只有以下条件全部满足，才进入第一支实现：

1. 阶段 9 已在 Windows x64 必需目标与 macOS 延期目标边界内收口，记录为 `develop@01142f7`；
2. 项目负责人确认快速页是否移除单参考素材；
3. 项目负责人明确批准第一支 `feature/local-json-persistence-foundation`；
4. 第一支的允许修改范围、测试范围、是否联网和费用上限明确记录；
5. 当前 Vidu 真实预算保持封存，不恢复流程 8 通道。

当前状态：第 1—5 项均已满足。项目负责人已批准 `feature/local-json-persistence-foundation`，允许普通工程联网并授权 Codex 按冻结门禁自行验收及在通过后非快进合并；本分支禁止真实 HTTP 和收费调用，因此实际费用为 0。

已冻结的第 2 项产品决策（2026-07-31）：

- 快速生图固定为纯文生图 `text_to_image`，不接收参考图或其他参考素材；
- 快速视频固定为纯文生视频 `text_to_video`，不接收参考图片、参考视频或其他参考素材；
- 参考图生图、图生视频及其他需要素材的功能继续进入对应专业页面，并由显式 `ProductFeature` 与受控素材槽位承载；
- 不得根据草稿中是否存在素材静默切换快速页协议或功能。

## 三、里程碑总览

| 里程碑 | 范围 | 目标 | 进入条件 | 退出条件 |
| --- | --- | --- | --- | --- |
| M0 | 启动决策 | 冻结产品取舍、分支权限和费用边界 | 阶段 9 收口 | 第一支获得明确批准 |
| M1 | 本地持久化基础 | Schema、revision、journal、恢复和迁移底座 | M0 | 基础门禁通过并合并 |
| M2 | 安全与后台契约 | 授权、Package、Registry、Feature、Context、Usage、Route、编排和迁移 | M1 | 新合同与迁移门禁通过并合并 |
| M3 | 后台服务与协议适配器 | Provider 管理、调用读模型和目标供应商适配器 | M2 | 合成 transport 全链路通过 |
| M4 | 后台集成验收 | 连接、模型、候选、提交、同步/异步调用记录统一闭环 | M3 | `provider-backend-integration-acceptance` 通过 |
| M5 | UI 后置接线 | 供应商、会话/上下文、创作、任务/调用记录 UI | M4 且逐支批准 | 可见 Electron 与 UI 回归通过 |
| M6 | 优化收口 | 完整回归、证据和下一步专项批准 | M5 | 负责人确认后停止，不自动进入真实 API |

## 四、分支顺序

### M1：本地持久化基础

1. `feature/local-json-persistence-foundation`

范围：应用/项目文件边界、Schema envelope、revision/CAS、原子写入、备份、journal、恢复扫描、幂等和历史迁移底座。

禁止：供应商页面、真实适配器、真实 HTTP、Vidu 预算恢复。

验收结果：实现提交 `f0e6ecc` 已通过 Node 178 项与 Vitest 395 项，共 573 项完整回归，以及类型检查、Lint、生产构建、平台审计、交接校验和恢复审计。工程记录见 `docs/active/多服务商功能路由-M1本地JSON持久化基础记录.md`。

### M2：安全与后台基础契约

按以下顺序逐支执行：

1. `feature/vidu-runtime-authorization-closure`
2. `feature/provider-package-connection-contracts`
3. `feature/provider-registry-atomic-catalog`
4. `feature/provider-runtime-authorization-contracts`
5. `feature/provider-feature-contracts`
6. `feature/project-conversation-context-snapshots`
7. `feature/provider-invocation-usage-contracts`
8. `feature/provider-execution-route-snapshot`
9. `feature/provider-text-streaming-contracts`
10. `feature/provider-public-candidates-orchestration`
11. `feature/provider-contracts-data-migration`

当前验收结果：

- 第一支实现提交 `b2e6d94` 已关闭流程 8 live-validation IPC 和 Electron 图片/视频新提交入口，移除 `passed + system_observed` 自动放行，并通过 Node 179 项与 Vitest 396 项、共 575 项完整回归及全部工程门禁；已通过 `975ef82` 非快进合并 `develop`。工程记录见 `docs/active/多服务商功能路由-M2-Vidu运行授权关闭记录.md`；
- 第二支实现提交 `44e2ce1` 已建立 `official | compatible_custom` Package/Template/Adapter、结构化 CredentialSchema、EndpointPolicy、安全模板 DTO、结构化加密凭证和补偿式原子连接保存合同，关闭任意 REST、协议猜测、未知 JSON 和旧任意 Provider/Connection 创建入口；已通过 Node 179 项与 Vitest 405 项、共 584 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-Package与连接合同记录.md`。
- 第三支实现提交 `d2688f9` 已增加单调 `registryRevision`、跨 Store 共享协调的 CAS/mutate、`present/missing/retired` 目录状态、精确 Model Definition/Profile、并发验证/同步/启停和 `declared` Profile 候选门禁；已通过 Node 179 项与 Vitest 409 项、共 588 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-Registry原子目录记录.md`。
- 第四支实现提交 `a91ca5b` 已增加 `RuntimeAccessPolicy`、应用级授权账本、最具体拒绝优先级、原子 claim、最大次数、过期、一次性 nonce、幂等键和在途 continuation 授权；已通过 Node 179 项与 Vitest 417 项、共 596 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-运行授权合同记录.md`。
- 第五支实现提交 `e60d1d6` 已冻结九类 ProductFeature、显式内部用途映射、ParameterSchema V2、`required_only | full` 投影、快速页纯文生图/视频和专业页单图约束，并拒绝未知/内部参数与任意 feature 字符串；已通过 Node 179 项与 Vitest 424 项、共 603 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-功能与参数合同记录.md`。
- 第六支实现提交 `1f0c04e` 已增加项目所属新会话、ConversationResponseDraft 的 conversation/message revision 与显式上下文选择、项目级会话/回复草稿原子仓储，以及固定 ProjectContext revision/contentHash/实际外发内容快照；已通过 Node 179 项与 Vitest 430 项、共 609 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-项目会话与上下文快照记录.md`。
- 第七支实现提交 `6ae59ff` 已增加媒体/文本统一 ProviderInvocationAttempt/Event、版本化 UsageSchema、ProviderUsageObservation/Summary、LocalResultObservation、四类聚合规则、七类 availability、项目级原子仓储和安全只读投影；已通过 Node 179 项与 Vitest 439 项、共 618 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-调用与用量合同记录.md`。
- 第八支实现提交 `6537d27` 已增加不可变 ProviderExecutionRouteSnapshot、项目级原子仓储和按提交时 `adapterKey + adapterVersion` 精确执行的 submit/query/cancel/receiveResult 分发框架，禁止读取当前默认路由或版本回退；已通过 Node 179 项与 Vitest 445 项、共 624 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-执行路由快照记录.md`。
- 第九支实现提交 `6c2edfb` 已增加不可变文本候选执行快照、ConversationResponseExecution、项目级连续事件仓储、受控流 DTO、取消/中断/恢复、renderer 背压断开与按序重放，以及应用退出持久化中断语义；已通过 Node 179 项与 Vitest 451 项、共 630 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-文本流式合同记录.md`。
- 第十支实现提交 `466fc7f` 已增加安全公开候选、稳定排序、短期一次性 routeSelectionToken、全量绑定事实指纹失效、项目元数据原子接受、授权 claim 协调、媒体/文本分离提交编排、幂等单提交及恢复决策；已通过 Node 179 项与 Vitest 459 项、共 638 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-候选与提交编排记录.md`。
- 第十一支实现提交 `27cd6d5` 已增加项目级追加式迁移读模型、源事实指纹幂等、`.bak` 恢复、快速页参考素材显式迁移、历史调用脱敏投影、`not_collected_legacy`、异步 `legacy_route_unavailable`、未绑定历史会话只读边界，以及 Provider/Connection 精确 Package ownership 和 Registry 单调恢复；已通过 Node 179 项与 Vitest 464 项、共 643 项完整回归及全部工程门禁。工程记录见 `docs/active/多服务商功能路由-M2-合同数据迁移记录.md`。

关键产出：

- 运行授权与流程 8 验证授权彻底分离；
- Provider/Connection/Model/Feature/Profile 具备版本化、原子和可恢复合同；
- Context、Usage、RouteSnapshot 和候选选择令牌成为主进程唯一事实；
- 旧 Draft、Task、Execution、Message、Context 和路由记录具备明确迁移或不可恢复状态；
- 不以模型名称、URL 或任意 JSON 自动猜测协议能力。

### M3：后台应用服务与协议适配器

以下分支在各自合同依赖满足后执行；可并行实现，但必须分别验收：

1. `feature/provider-management-framework`
2. `feature/provider-invocation-read-model`
3. `feature/deepseek-chat-adapter`
4. `feature/volcengine-doubao-vision-adapter`
5. `feature/volcengine-seedance-video-adapter`
6. `feature/kling-video-adapter`
7. `feature/newapi-provider-package`
8. `feature/vidu-provider-package-migration`

每个适配器必须先完成官方合同证据、参数映射、合成成功/失败响应、取消、结果解析和 UsageSchema 测试。真实联网、凭证验证和收费测试不包含在本计划默认授权内。

当前验收结果：

- 第一支实现提交 `0e532ac` 已增加安全模板读取、Package-owned 连接创建、结构化凭证轮换、精确无费用验证、目录同步、精确手工模型登记、连接/模型启停、活跃 operation 删除门禁、软删除和应用级管理审计；目录消失、模型停用和连接停用会关闭相关路由偏好，未迁移 legacy 记录不能通过通用管理框架修改。完整门禁为 Node 179 项与 Vitest 472 项，共 651 项通过，0 失败、0 跳过；真实 HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M3-供应商管理框架记录.md`。
- 第二支实现提交 `cf51cfe` 已增加跨项目媒体/文本调用安全读模型、项目/功能/Provider/Connection/Model/状态/时间筛选与分页、脱敏详情、提交时显示名快照、精确 UsageSchema 解释、本地结果与 Work 登记状态，以及命名 Electron IPC/preload 查询端口；公开 DTO 不包含 RouteSnapshot ID、Package/Adapter、Endpoint、凭证、Prompt、路径、Hash、远端 operation 或原始响应。完整门禁为 Node 179 项与 Vitest 475 项，共 654 项通过，0 失败、0 跳过；Windows 生产 Electron 新增 4 个进程、4/4 响应、1 个可见窗口、关闭后残留 0；真实 HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M3-调用读模型记录.md`。
- 第三支实现提交 `eff72e5` 已增加版本化 DeepSeek Package、官方固定 Origin 管理适配器、普通文本/推理精确参数投影、data-only SSE、内容白名单、取消、应用退出中断、用户显式新 attempt 恢复决策，以及六项 token UsageSchema；远端响应 ID、推理内容、URL、凭证、模型、Prompt 和响应正文不进入公开结果或安全日志，任何失败均不自动重试或切换服务商。完整门禁为 Node 179 项与 Vitest 485 项，共 664 项通过，0 失败、0 跳过；真实 HTTP、真实凭证和收费调用均为 0。官方证据见 `docs/active/多服务商功能路由-M3-DeepSeek官方合同证据.md`，工程记录见 `docs/active/多服务商功能路由-M3-DeepSeek文本适配器记录.md`。
- 第四支实现提交 `2ee9b3e` 已增加版本化 Volcengine Ark Package、固定官方 Origin、无免费验证的精确 Endpoint/Model 登记模板、豆包视觉双功能 Profile 工厂、单张受控 Base64 图片、严格四分类 JSON Schema、图片转提示词本地固定派生、五项 token UsageSchema、审核/fallback 失败关闭、单请求取消和应用退出中断；不按模型名称猜能力，不公开远端响应 ID、实际模型名、审核标签或原始响应，任何失败均不自动重试或切换服务商。完整门禁为 Node 179 项与 Vitest 495 项，共 674 项通过，0 失败、0 跳过；真实 HTTP、真实凭证和收费调用均为 0。官方证据见 `docs/active/多服务商功能路由-M3-豆包视觉官方合同证据.md`，工程记录见 `docs/active/多服务商功能路由-M3-豆包视觉适配器记录.md`。
- 第五支实现提交 `f905ade` 已增加 Seedance 异步视频 Adapter/Protocol、动态精确 Profile/ParameterSchema、GET/POST/DELETE 任务 Runtime、纯文本视频与单张受控首帧视频、六态查询、取消、终态 Usage、24 小时签名 URL 内存快照、受控下载和原 RouteSnapshot 重启 attach；不按模型名称猜能力，不发送任意 URL、多图、首尾帧、多模态参考、tools、未知 JSON 或隐私标识，不公开远端模型、失败正文、签名 URL 或原始响应，任何失败均不自动重试或切换服务商。完整门禁为 Node 179 项与 Vitest 510 项，共 689 项通过，0 失败、0 跳过；真实服务商数据面 HTTP、真实凭证和收费调用均为 0。官方证据见 `docs/active/多服务商功能路由-M3-Seedance官方合同证据.md`，工程记录见 `docs/active/多服务商功能路由-M3-Seedance视频适配器记录.md`。
- 第六支实现提交 `8b02a40` 已增加版本化 Kling API 2.0 Package/Adapter/Protocol、动态精确 Profile/ParameterSchema、纯文本视频与单张受控首帧视频、单任务查询、四态映射、Billing Usage、30 天结果失效、受控下载和原 RouteSnapshot 重启 attach；官方未发布取消端点，因此取消固定不发 HTTP 并保持远端 processing。实现不按模型名称猜能力，不发送任意 URL、多图、首尾帧、回调、外部任务 ID、tools 或未知 JSON，不公开任务、模型、失败正文、防盗链 URL 或原始响应，任何失败均不自动重试或切换服务商。完整门禁为 Node 179 项与 Vitest 523 项，共 702 项通过，0 失败、0 跳过；真实服务商数据面 HTTP、真实凭证和收费调用均为 0。官方证据见 `docs/active/多服务商功能路由-M3-Kling官方合同证据.md`，工程记录见 `docs/active/多服务商功能路由-M3-Kling视频适配器记录.md`。
- 第七支实现提交 `3bd21c1` 已增加 NewAPI compatible Package、必填自定义 `/v1` Base URL、结构化 API Key、Chat/Image/Video 三个适配器、动态精确 Model Definition/Profile/ParameterSchema 与共享安全 Runtime；Package 不预置模型，不按目录名称猜能力，快速生图和文生视频保持纯文本，专业图生视频只允许一张受控首帧。完整门禁为 Node 179 项与 Vitest 542 项，共 721 项通过，0 失败、0 跳过；真实服务商数据面 HTTP、真实凭证和收费调用均为 0。官方证据见 `docs/active/多服务商功能路由-M3-NewAPI官方合同证据.md`，工程记录见 `docs/active/多服务商功能路由-M3-NewAPI服务商包记录.md`。
- 第八支实现提交 `f7f4cc6` 已把 C2 Vidu 迁移为单一官方 Package、三个精确 Adapter/Protocol、10 个冻结 Model Definition/Profile、ParameterSchema V2、空指标 UsageSchema 和原 RouteSnapshot 薄适配层；迁移幂等保留连接与凭证引用，Image V1 固定禁用，Gemini 图片与 Q3 视频默认 restricted，Electron 旧五条网络入口全部硬关闭。完整门禁为 Node 179 项与 Vitest 546 项，共 725 项通过，0 失败、0 跳过；Windows Electron 新增 4 个进程、4/4 响应、1 个可见窗口、关闭后残留 0；真实 Vidu HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M3-Vidu服务商包迁移记录.md`。

### M4：后台与适配器集成验收

1. `feature/provider-backend-integration-acceptance`

验收结果：实现提交 `ebac373` 已完成 Registry 驱动候选、10 个阶段 9 适配器精确派发、统一异步恢复、共享受控传输地址冻结和纯合成闭环。完整门禁为 Node 179 项与 Vitest 561 项，共 740 项通过，0 失败、0 跳过；真实 HTTP、真实 DNS、真实凭证和收费均为 0。工程记录见 `docs/active/多服务商功能路由-M4-后台集成验收记录.md`。

必须覆盖：

- 官方连接与兼容自定义连接的安全合同；
- 模型发现、精确 Profile、启停和候选选择；
- 快速页纯文本输入、快速/专业参数投影，以及专业图生图/图生视频单图约束；
- 对话、项目上下文和媒体外发快照；
- 同步、异步、文本调用的状态、取消、恢复和用量完整性；
- 全部使用合成 transport，真实 Token、网络和收费次数为 0。

该分支通过前，不得启动任何 `feature/ui-*` 分支。

### M5：UI 后置接线

各支已由项目负责人 2026-08-03 的连续托管决定统一批准，但仍须逐支实现、验收、推送和非快进合并：

1. `feature/ui-provider-management-wiring`
2. `feature/ui-conversation-context-wiring`
3. `feature/ui-image-feature-wiring`
4. `feature/ui-video-feature-wiring`
5. `feature/ui-task-call-records-wiring`
6. `feature/ui-provider-acceptance-closeout`

UI 只消费已合并的安全 DTO、ParameterSchema、选择令牌、调用读模型和提交端口，不在 React 中新增模型名称、协议判断、usage 字段路径或适配器分支。

当前验收结果：

- 第一支实现提交 `8d845a9` 已把 Provider IPC、preload 和供应商页面统一接到 Package Template 与 `ProviderManagementFramework`，提供官方/兼容连接、结构化凭证写入与轮换、验证、目录同步、精确模型登记、连接/模型启停和本地软删除；在线管理 Adapter Registry 保持为空，真实 API 动作显示为等待专项批准并禁用。完整门禁为 Node 179 项与 Vitest 562 项，共 741 项通过，0 失败、0 跳过；Windows Electron 隔离烟测新增 4 个进程、4/4 响应、1 个可见窗口、关闭后残留 0；真实 HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M5-供应商管理UI接线记录.md`。
- 第二支实现提交 `4e55567` 已把对话页接到项目范围 Conversation、不可变 ProjectContext 快照、文本候选选择令牌、外发确认、文本提交、流式事件重放、取消和恢复读取端口；旧应用级对话保持只读，可按安全条件完整复制到当前项目。完整门禁为 Node 179 项与 Vitest 563 项，共 742 项通过，0 失败、0 跳过；Windows Electron 隔离烟测在 `1080x720` 新增 4 个进程、4/4 响应、1 个可见窗口、关闭后残留 0；真实 HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M5-对话与上下文UI接线记录.md`。
- 第三支实现提交 `1ed2772` 已把快速生图和专业生图接到安全图片候选、`ParameterSchema`、一次性路由选择令牌、外发确认和受控提交端口。快速生图固定为纯文生图，旧带图草稿只能确定性迁移到专业图生图；专业页显式区分文生图与恰好单图的图生图，并只接受固定 revision 的 ProjectContext。权威最小窗口同步落实为 `800x720`。完整门禁为 Node 185 项与 Vitest 571 项，共 756 项通过，0 失败、0 跳过；Windows Electron 隔离烟测新增 4 个进程、4/4 响应、1 个可见窗口、关闭后残留 0；真实 HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M5-图片功能UI接线记录.md`。
- 第四支实现提交 `574eafc` 已把快速视频、文生视频和图生视频接到安全视频候选、`ParameterSchema`、一次性路由选择令牌、外发确认和受控提交端口。快速视频固定为纯文生视频且无素材/上下文入口；文生视频只接受固定 ProjectContext，图生视频只接受恰好一张受控图片，旧草稿必须显式迁移或清理。仅注册现有 Vidu 包中合同完整的 `image_to_video` Schema，不伪造其他服务商或文生视频能力。完整门禁为 Node 188 项与 Vitest 581 项，共 769 项通过，0 失败、0 跳过；Windows Electron 隔离烟测新增 4 个进程、4/4 响应、1 个可见窗口、关闭后残留 0；真实 HTTP、真实凭证和收费调用均为 0。工程记录见 `docs/active/多服务商功能路由-M5-视频功能UI接线记录.md`。

## 五、单支执行门禁

每支分支都必须完成以下闭环：

1. 从包含全部依赖的最新 `develop` 创建；
2. 只修改该支声明的合同、适配器、测试和工程记录；
3. 补齐成功、失败、取消、恢复、并发、迁移和安全边界测试；
4. 运行：

   ```text
   npm.cmd test
   npm.cmd run typecheck
   npm.cmd run lint
   npm.cmd run build
   npm.cmd run audit:platform
   npm.cmd run verify:handoff
   npm.cmd run verify:recovery-audit
   git diff --check
   ```

5. 涉及 Electron、preload 或 UI 时增加 Windows Electron 启动、可见窗口和清理烟测；
6. 更新对应工程记录、证据和 `PLANS.md`；
7. Codex 按冻结门禁自行验收；通过后非快进合并 `develop`，保留本地与远程功能分支；
8. 合并后重新读取最新 `develop` 再启动下一支；任一停止条件触发时中止连续执行并记录阻断。

## 六、联网与数据安全闸门

- M1-M4 默认只允许临时目录、内存 transport、合成响应和零网络测试；
- 真实服务商请求、凭证验证、费用和用量验证必须建立新的专项批准，不由本计划继承；
- Vidu 两项已用尽预算不得恢复，Image V1 不得因本计划晋级；
- Token、API Key、绝对私有路径、原始响应、签名 URL、Prompt、媒体和原始日志不得进入 Git 或证据；
- 任何未知提交结果必须进入 `unknown_outcome`，禁止自动重试；
- 任何未完成本地校验的远端结果不得登记 Work。

## 七、停止条件

出现以下任一情况，立即停止当前支，不增加调用次数或静默切换协议：

- 依赖分支未合并或最新 `develop` 事实不一致；
- 迁移可能覆盖历史、丢失数据或无法恢复；
- DTO、日志、诊断或证据泄漏凭证、路径、Hash、Prompt 或原始响应；
- 适配器只能靠模型名称猜测能力，或把未知 JSON 当作兼容协议；
- 测试出现失败、未解释跳过、假进度、假用量或假 Work；
- 需要真实联网、凭证或收费但没有新的专项批准。

## 八、当前下一步

M5 第四支 `feature/ui-video-feature-wiring` 已按批准边界实现并通过自验收。下一步提交工程记录、推送并非快进合并该支；随后从最新 `develop` 创建 M5 第五支 `feature/ui-task-call-records-wiring`。该支只把任务中心接到已合并的安全调用读模型、状态、时间线、用量完整性和本地结果事实，不公开受保护的路由、凭证、Prompt、远端标识或文件系统事实。

项目负责人已批准阶段 10 之前 M2—M6 的连续托管执行；关机设定已取消。后续不等待中途手工确认，但不扩大到阶段 10、macOS 实机与媒体工具链、真实服务商 HTTP、真实凭证读取/验证或收费调用。
