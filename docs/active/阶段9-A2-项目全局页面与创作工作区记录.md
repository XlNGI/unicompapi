# 阶段 9 A2｜项目、全局页面与创作工作区记录

日期：2026-07-28

分支：`feature/cross-platform-workspace-ui`

状态：项目负责人 Windows 手工验收通过；已通过 `a6271c3` 合并 `develop`，功能分支本地与远程均保留。

## 实际修改

- 保留 `AppLayout` 为唯一主内容地标，移除对话页和模型与服务商页的嵌套 `main`；
- 模型与服务商页读取真实 `getCredentialStatus`，分别展示未配置、已安全保存、验证中、有效、无效、已删除、安全存储不可验证和读取失败；
- 凭证继续只写入本机安全存储，不提供读取、复制或明文回显；
- 更新图片五页和视频前三页的空草稿/阻断说明，移除已经过期的 B2、B3、B4 和 A1“尚未接线”文案；
- 无真实后台适配器时继续明确阻断，不创建假任务、假进度或假作品；
- 新增 A2 跨平台页面契约测试，锁定唯一主地标、受控 DTO/IPC、异常状态和页面无平台分支。

## 验证结果

- A2 目标契约测试 20 项通过；
- 合并最新 B3 与对话上下文 IPC 后，Node UI、IPC 与工具链测试 153 项通过；
- Vitest 领域与平台测试 319 项通过；
- 合计 472 项通过，0 失败、0 跳过；
- `pnpm.cmd typecheck`、`pnpm.cmd lint`、`pnpm.cmd build` 通过；
- 平台审计扫描 197 个生产侧文件，23 处已登记运行时访问、49 处已登记平台字面量，0 项违规；
- 权威交接包 50 条校验和与 27 个资源 Hash/字节全部匹配；
- Windows 运行时快捷键、direct/system 代理、电源状态和 FFmpeg 8.1.2 开发工具链检查通过；
- `git diff --check` 通过；
- 本地浏览器 1280×720 下检查项目、对话、模型与服务商、快速生图和快速视频页面：唯一主内容地标、无横向溢出、深浅主题可切换，控制台 0 错误、0 警告。

## 未完成与边界

- 本次不包含 A3 视频编辑与本地设置工作；
- 当前未执行 macOS 真实设备验收，不得将 Windows 工程结果写成跨平台实机通过；
- 浏览器环境没有 Electron 项目目录选择器、安全存储和真实后台适配器，因此这些能力只验证了受控接口、真实阻断状态和自动化契约；
- A2 已合并，但 macOS 与其余阶段 9 实机阻断项没有因此变为通过。
