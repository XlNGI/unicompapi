# 阶段 1｜UI 对照记录

日期：2026-07-20
负责范围：开发者 B｜页面骨架、Design Tokens 与基础组件

## 一、依据

- `docs/01-信息架构与左侧导航最终规范-V1.2.0.md`
- `docs/02-正式页面结构与业务边界总说明-V1.2.0.md`
- `docs/03-组件与交互状态最终规范-V1.2.0.md`
- `docs/04-视觉系统与Design-Tokens最终说明-V1.2.0.md`
- `docs/05-Codex实现与验收说明-V1.2.1.md`
- `docs/06-已废止内容清单-V1.2.0.md`
- `tokens/UniComp-AI-Design-Tokens-V1.2.0.json`
- `resources/高保真页面/` 下对应权威设计图

以上路径均相对于 V1.2.1 最终 UI 与开发交接包。

## 二、本次已实现

- 建立八个一级页面组件：对话、项目、图片创作、视频创作、任务中心、作品库、模型与服务商、本地设置。
- 每个页面包含标题、阶段标签、业务边界说明和空状态占位。
- 建立 `Button`、`Card`、`EmptyState`、`StatusPill` 四个基础组件。
- 建立第一版 CSS Design Tokens，覆盖深色/浅色基础颜色、文本、边框、强调色、状态色、字体、间距、圆角、阴影与动效时长。
- 已接入 A 的桌面壳和导航代码，并通过 `NavigationItemId` 到页面组件的完整类型映射实现八页切换。
- 已建立无面包屑全局上边栏，包含品牌、三态主题切换和平台对应的窗口控制。
- 桌面壳、导航与页面样式已统一迁移到语义 Design Tokens，支持深色和浅色整体切换。
- 已建立图片创作 5 项、视频创作 4 项二级页面的空状态组件和统一页面参数；组件只表达入口边界，不创建任务、不读取未明确选择的素材。
- 阶段 2 基于领域契约补充空白、加载、失败、过期、服务不可用、文件丢失、只读和恢复状态夹具；状态预览位于“本地设置”，不访问后台或文件系统。
- 加载、失败、过期、文件丢失、只读和断开状态直接使用领域契约值，UI 未复制状态机或状态转换规则。

## 三、边界对照

- 未新增“首页”，一级页面保持最终八项结构。
- 对话页仅用于问答、分析和项目上下文整理，没有直接生成入口。
- 未恢复多图参考、图片批量创作或视频批量创作。
- 未加入登录、会员、充值、云同步或账号商业元素。
- 未写死服务商、模型、价格、费用、时长、分辨率、数量或虚假进度。
- 任务中心与作品库文案明确区分草稿、任务、远端结果和本地正式作品。
- 未接入后台接口；Electron 主进程和预加载脚本仅增加受控的窗口控制 IPC，没有暴露通用 Node 能力。

## 四、暂未对齐

- 当前一级页面和二级页面均为阶段 1 空状态骨架，尚未实现页面业务区、覆盖层或真实状态流转。图片与视频二级导航已接入桌面壳：进入创作域时展开并选中首项，切换子项时保持展开，点击已展开父级时保持展开并回到首项，切换到其他一级页面时收起。
- Windows 已完成人工页面对照、主题交互和常见窗口尺寸验证；未建立自动像素差异基线，macOS 视觉一致性留待阶段 9 实机验收。
- 当前组件只覆盖阶段 1 所需最小变体，完整组件状态矩阵留待后续阶段补齐。
- Electron 窗口已在 Windows x64 环境实际启动并完成人工验收；Windows 截图证据已归档，macOS 实机证据暂缓。

## 五、后续验收建议

1. 对照 `resource-manifest.json` 确认每个页面使用的权威设计图版本。
2. 分页面补齐空白、加载、失败、过期、服务不可用和本地文件异常等适用状态。
3. 在 Windows 和 macOS 上验证核心信息架构、主题和字体回退一致性。

## 六、验证结果

- `npm run typecheck`：通过。
- `npm run lint`：通过，项目已建立 ESLint 9 flat config。
- `npm run build`：通过，Vite 共转换 73 个模块，Electron TypeScript 构建完成。
- 八项一级导航均有对应页面组件，映射由 TypeScript `Record<NavigationItemId, ComponentType>` 完整性约束。
- `npm test`：UI 11 项与领域 16 项全部通过；UI 新增状态顺序、领域值引用、加载、错误、只读、禁用和焦点合约验证。
- 阶段 2 状态预览：浏览器实际验证 8 种状态、领域状态值、加载/错误/只读语义、本地操作反馈及深浅主题；控制台无警告或错误。
- 二级导航交互：浏览器实际验证“专业生图 → 再次点击图片创作”后保持展开并回到“快速生图”；分组可访问名称为“图片创作二级导航”，控制台无警告或错误。
- Electron 窗口：通过 `pnpm run dev` 实际启动，检测到标题为 `UniComp` 且处于响应状态的窗口进程。
- 全局上边栏：Windows 实际启动时已枚举到可见 `UniComp` 主窗口；静态类型、lint 和构建通过。
- 页面截图：Windows 验收截图共 4 张，归档于 `docs/active/evidence/phase1/windows/`。

## Windows 人工验收

日期：2026-07-21
系统：Windows
提交：fc475ce

- [x] 八项一级导航
- [x] 图片创作五项二级导航
- [x] 视频创作四项二级导航
- [x] 导航高亮与展开规则
- [x] 深色主题
- [x] 浅色主题
- [x] 跟随系统主题
- [x] 主题持久化
- [x] 窗口最小化、最大化、还原与关闭
- [x] 标题栏拖动
- [x] 键盘导航与焦点
- [x] 常见窗口尺寸检查
- [x] 禁止功能检查

macOS：本阶段暂缓，未执行实机验收，不标记为通过。

Windows 验收证据：`docs/active/evidence/phase1/windows/`，共 4 张 PNG。

## 阶段 3｜A1 项目页待验收记录

日期：2026-07-22
分支：`feature/project-center-ui`

- 项目页已接入开发者 B 提供的 `listProjects`、`createProject`、`openProject`、`closeProject` 与 `getProjectSession`，未向渲染进程暴露项目路径或通用文件系统。
- 已实现当前项目、最近项目、最近任务、最近作品和图片/视频创作入口；最近任务和作品直接使用开发者 B 的全局读模型，不伪造记录或状态。
- 无活动项目时，图片和视频创作入口会要求先新建或打开项目，不创建孤立任务或作品。
- 默认页面已从“对话”调整为“项目”，八项一级导航顺序未改变。
- 已覆盖加载、空白、项目失效、损坏、只读、断盘和操作失败提示；读模型项目级问题不会被隐藏为成功空白。
- 浏览器完成浅色、深色、页面滚动和禁止入口验证，控制台无警告或错误；生产模式 `UniComp` 窗口可正常启动并响应。
- `npm test`：UI/契约 17 项、领域与平台 55 项通过；`npm run typecheck`、`npm run lint`、`npm run build` 均通过。
- 本记录为开发者 A 自测结果，尚未上传远程，等待项目负责人进行 Windows 人工验收。

## 阶段 3｜A2 任务中心待验收记录

日期：2026-07-22
分支：`feature/task-center-ui`

- 任务中心直接使用 `listTasks()` 与 `getTaskDetails(taskId)`，提供任务搜索、项目筛选、状态筛选、任务列表和详情层。
- 展示任务的真实执行阶段、执行次数、重试性、原始输入和最终提示词；不显示推算进度、排队时间或费用。
- 项目失效、断盘或仓储损坏时保留项目级问题提示，不隐藏为成功空白。
- 提供返回来源项目和进入作品库的导航；重试、取消、日志和具体作品定位尚无受控 IPC，因此不提供无效操作按钮。
- `npm test`：UI/契约 22 项、领域与平台 59 项通过；`npm run typecheck`、`npm run lint`、`npm run build` 均通过。
- 浏览器已验证空状态、搜索、状态筛选和深浅主题，控制台无错误；生产模式 `UniComp` 窗口可正常启动并响应。
- 开发者 A 自测与项目负责人 Windows 人工验收均已通过，准备提交并上传原功能分支。

## 阶段 3｜A3 作品库待验收记录

日期：2026-07-22
分支：`feature/work-library-ui`

- 作品库直接使用 `listWorks()` 与 `getWorkDetails(workId)`，提供作品搜索、项目/媒体/文件状态筛选、作品列表和详情层。
- 预览通过短期 `createWorkMediaHandle(workId)` 获取，不拼接或显示本地路径；系统定位与恢复分别使用 `revealWorkFile(workId)` 和 `relinkFile(fileId)`。
- 文件丢失、只读、断盘和损坏记录继续展示，并提供真实的重新定位入口；未增加通用文件浏览、导入、删除或覆盖历史版本能力。
- 详情展示来源项目、来源任务、来源执行、父版本、文件大小与校验时间，均来自开发者 B 的受控 DTO。
- `npm test`：UI/契约 26 项、领域与平台 59 项通过；`npm run typecheck`、`npm run lint`、`npm run build` 与 `git diff --check` 均通过。
- 浏览器已验证空状态、搜索、媒体筛选、主题与 1280×720 无横向溢出，控制台无警告或错误；当前不上传、不合并。

## 阶段 3｜A4 对话页待验收记录

日期：2026-07-22
分支：`feature/chat-page-ui`

- 按权威三栏结构实现对话列表、对话内容与项目上下文草稿区域，明确显示阶段 3 的“离线 / 服务未配置”真实状态。
- 当前输入和附件名称只保存在页面组件状态中，不上传、不持久化、不进入项目或创作页；关闭或刷新页面后即清空。
- 未伪造历史对话或 AI 回复，未加入生成图片、生成视频或提交任务入口；发送按钮在服务未配置时禁用。
- 项目上下文区读取当前项目 Session，但没有内容选择与保存契约时保持空白并禁用保存，避免伪造落库成功。
- `npm test`：UI/契约 30 项、领域与平台 59 项通过；`npm run typecheck`、`npm run lint`、`npm run build` 与 `git diff --check` 均通过。
- 浏览器已验证三栏结构、输入与清空、发送/保存禁用、控制台无警告或错误；在项目最小窗口 1080×720 下工作区可正常向下滚动且无横向溢出。
- `npm start` 已启动生产版 `UniComp` 窗口并读到完整项目页面结构；当前不上传、不合并，等待项目负责人人工验收。
