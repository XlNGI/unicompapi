# 阶段 9 B3 Windows 运行时与媒体证据摘要

源提交：`7aac020`

目标：`windows-x64-primary`

运行时：Windows `10.0.19045` x64、Electron `33.4.11`、Node `20.18.3`

## 已通过

- Node UI/IPC/工具链测试 143 项，Vitest 领域/平台测试 279 项，合计 422 项，0 失败、0 跳过；
- TypeScript、ESLint、生产构建、平台审计、权威交接包校验和差异检查通过；
- 平台审计扫描 184 个生产侧文件，登记 23 处直接运行时平台访问和 49 处平台字面量，0 违规；
- 结构化参数不经 shell，超时有显式状态；Windows 使用受控 `taskkill /T /F` 终止进程树，真实父子进程测试结束后无存活后代；
- 活动视频导出在生命周期中断时进入 `recovery_required`，不创建 Work、不重复完成；
- 代理视频、缩略图、波形和正式软件导出共用受控进程监督器，取消、退出和异常均清理临时文件；
- Windows 生产构建隐藏启动新增 4 个 Electron 进程，4/4 保持响应；向主窗口发送关闭消息后残留进程为 0；
- Electron `safeStorage` 真实可用，密文字节数 95，未持久化明文；
- Electron 原生快捷键注册/释放、direct/system 代理解析、通知能力探测和电源状态探测通过；
- 已批准 Windows FFmpeg `8.1.2` LGPL-only 开发/测试工具链验证通过；真实 probe、预览、缩略图、波形、软件导出、进度、取消、组合导出和独立输出校验通过；
- renderer 外链只允许无凭证 HTTPS URL；退出时撤销短期媒体句柄并释放快捷键、代理凭证引用、媒体进程和休眠阻止器。

## 未执行或未完成

- 未让真实 Windows 设备进入睡眠、唤醒、锁屏和解锁，因此这些实机用例保持 `not_run`；
- 未实际展示系统通知、播放通知声音或打开系统设置页面，因此只确认能力探测，投递/声音/跳转保持 `not_run`；
- Windows 原生目录选择器人工授权/撤销仍沿用 B2 的 `not_run`；
- macOS 进程、代理、通知、快捷键、电源和媒体实机证据全部为 `not_run`；
- macOS 本地开发/测试媒体工具链仍未获单独批准，不得安装、绑定或把 Windows 结果替代为 macOS 通过；
- `.tools/`、FFmpeg 二进制和下载包未进入 Git、生产构建或发布物。
