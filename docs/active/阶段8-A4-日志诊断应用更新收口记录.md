# 阶段 8 A4｜日志诊断、应用更新与全页收口记录

日期：2026-07-27
分支：`feature/local-settings-diagnostics-update-ui`
基线：`be62513 Merge phase 8 B4 local settings diagnostics update`

## 范围

- 完成“日志与诊断”“应用更新”两个本地设置分类面板。
- 接入 B4 的维护状态、诊断包预览/生成、日志位置打开、更新状态检查、便携设置导出/导入预检、全部恢复默认和清除本机应用数据计划。
- 更新页只显示真实能力、版本、阻断、完整性、签名和不可用原因；没有安装、修复、回退执行按钮。
- 清除本机应用数据与恢复默认设置分开展示，并在确认计划里显示预计文件、容量、设置重置、凭证删除、项目排除和外部文件排除。

## 实现说明

- `src/pages/settings/SettingsPage.tsx`
  - 新增 A4 面板、维护状态加载、诊断包预览/生成、便携导入导出和清除数据计划交互。
  - 最后两个分类从待接入状态切换为真实 B4-backed UI。
  - 右侧状态摘要补充诊断和更新事实。
- `src/styles/pages.css`
  - 新增诊断列表、清除范围、便携 JSON 文本框和更新项卡片的响应式样式。
- `tests/ui/settings-maintenance-panels.test.mjs`
  - 固化 A4 UI 契约，防止绕过 B4 IPC、混淆重置与删除、或加入假更新执行控件。

## 验证结果

- `pnpm.cmd typecheck`：通过。
- `pnpm.cmd lint`：通过。
- `pnpm.cmd test`：通过，Node/UI 134 项 + Vitest 253 项，共 387 项，0 失败。
- `pnpm.cmd build`：通过。
- `git diff --check`：通过；仅有 Windows LF/CRLF 提示，不是空白错误。
- Windows Electron 启动烟测：通过，新启动 4 个 Electron 进程，均响应/可存活，已清理。

## 项目负责人验收与合并结果

- 项目负责人已完成 Windows Electron 手工验收并确认通过。
- A4 已通过 `3b06a1c Merge phase 8 A4 local settings diagnostics update UI` 合并 `develop`。
- `feature/local-settings-diagnostics-update-ui` 本地与远程分支保留。
