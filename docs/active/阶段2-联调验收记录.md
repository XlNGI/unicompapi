# 阶段 2｜联调验收记录

日期：2026-07-21
基线：`develop` / `1ae5b84`

## 已通过

- `npm test`：UI 11 项、领域与平台 21 项，共 32 项通过。
- `npm run typecheck`、`npm run lint`、`npm run build`：通过；Vite 转换 73 个模块。
- Windows Electron：生产构建主窗口成功启动，标题为 `UniComp`，进程响应正常。
- UI 状态预览：8 种状态可见；领域状态值、加载、错误、只读语义、本地操作反馈和工作区滚动通过；浏览器无警告或错误。
- 本地存储契约：安全相对路径、固定目录、文件索引更新/冲突检查及仓储端口由自动化测试覆盖。
- Windows 文件系统适配器：真实临时目录内的 JSON 首次写入、原子替换读取、嵌套目录、幂等删除和临时文件清理通过。
- 恢复契约：文件丢失、只读和外置存储断开状态转换由自动化测试覆盖。
- 现有 IPC：仅暴露平台标识和窗口控制；`contextIsolation` 已开启，`nodeIntegration` 已关闭，未向渲染进程暴露通用 Node 能力。

## 尚未通过

- 项目、草稿、素材、任务和作品仓储尚未实现。
- SHA-256 流式校验、状态探测、恢复计划、校验结果持久化、安全 relink 和索引自动重建已通过临时目录测试；备份恢复、重新下载和 Electron 恢复联调尚未实现。
- 最小受控存储 IPC、preload 白名单和主进程控制器已实现并通过注入 session 的平台测试；真实项目打开流程尚未向主进程设置活动项目 session，因此 UI 端到端调用仍待后续联调。
- macOS 实机验收按计划移至阶段 9。

## 结论

阶段 2 当前为部分通过。继续补齐活动项目 session 接入、剩余恢复执行器和 Windows Electron 端到端联调；完成后重新执行本记录全部门禁。本次结果提交项目负责人验收，负责人确认前不合并 `main`，不启动阶段 3。
