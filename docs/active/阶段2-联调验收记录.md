# 阶段 2｜联调验收与收口记录

日期：2026-07-22
基线：`develop` / `715cccd`
验收人：项目负责人兼开发者 B

## 一、阶段目标结论

阶段 2“本地领域基础”工程门禁通过，正式收口。

本阶段已建立不依赖在线服务的领域、存储、文件恢复和受控主进程基础，可供阶段 3 全局页面复用。阶段 3 不得绕过这些契约另建项目路径、文件状态或恢复逻辑。

## 二、已通过

- 项目、草稿、素材、文件引用、任务、执行记录和作品实体边界已建立。
- 任务、执行和文件状态机覆盖失败、取消、重试、过期、丢失、只读、断盘、损坏和恢复语义，并拒绝非法转换。
- 安全相对路径、固定项目目录、原子 JSON 写入、JSON 仓储、派生文件索引和索引重建已通过 Windows 临时目录测试。
- SHA-256 流式校验、真实字节进度、状态探测、校验结果持久化和安全 relink 已实现。
- 备份恢复执行器要求用户确认及既有 Hash 证据，校验失败不会覆盖目标文件。
- 受控存储 IPC 仅按 `fileId` 提供探测、校验、relink、备份恢复和索引重建；renderer 不提交项目根目录、备份路径或任意目标路径。
- 活动项目 Session 支持主进程原生目录选择、`project.json` 校验、查询、切换协调和关闭。
- 项目页面已接入 Session API；Windows 可见 Electron 窗口完成有效项目目录打开、项目名称展示和关闭项目联调。
- `contextIsolation` 开启、`nodeIntegration` 关闭，preload 未暴露通用 Node 或文件系统能力。
- 基础壳在没有在线服务时可运行，不包含登录、会员、充值或云同步。

## 三、最终自动化门禁

- UI 与 IPC 安全契约：14 项通过。
- 领域与平台测试：52 项通过。
- 总计：66 项测试通过，0 项失败。
- `npm.cmd run typecheck`：通过。
- `npm.cmd run lint`：通过。
- `npm.cmd run build`：通过；Vite 转换 73 个模块。
- `git diff --check`：通过。

## 四、明确后置事项

以下事项不阻塞阶段 2 收口，也不得在本记录中伪标记为已完成：

1. 新建项目页面与完整全局页面流程：进入阶段 3。
2. 文件恢复 UI 和明确的用户操作入口：由开发者 A 在阶段 3 基于现有受控 IPC 实现。
3. 真实备份来源、保留周期和清理策略：后续产品与存储任务决定。
4. 重新下载执行器：等待后台服务能力和鉴权契约，最早在阶段 4 后实现。
5. macOS 文件系统、原子替换和实机 UI 验证：按计划进入阶段 9。
6. Schema 迁移：在出现第二版持久化结构前单独设计，不在阶段 2 猜测。

## 五、负责人决策

项目负责人于 2026-07-22 确认：

- 阶段 2 正式通过并关闭；
- 允许开始阶段 3 任务拆分与开发准备；
- 后续功能分支继续从最新 `develop` 创建；
- 不合并到 `main`，直到后续阶段满足对应发布门禁。
