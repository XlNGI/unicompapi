# 阶段 4｜模型与服务商联调验收记录

日期：2026-07-22
联调基线：`b487184`（开发者 A 页面合并后的 `develop`）
收口分支：`feature/phase4-integration-closeout`

## 一、联调范围

- 服务商、连接、模型、能力证据与路由偏好领域契约；
- 本机安全凭证库与 Electron `safeStorage`；
- 服务商与连接创建、更新、启停和软删除；
- 模型登记、启停、目录同步与能力验证端口；
- 凭证保存、替换、本地检查和本地删除；
- 默认用途、备选顺序和提交前路由确认；
- 模型与服务商页面及真实离线状态；
- Windows Electron preload/IPC 可见窗口联调。

## 二、联调结果

### 自动化门禁

- UI、IPC 与安全契约测试：39 项通过；
- 领域与平台测试：72 项通过；
- TypeScript：通过；
- ESLint：通过；
- Vite 与 Electron 生产构建：通过；
- Git 差异格式检查：通过。

### Windows 可见窗口

- 在独立临时 `userData` 下启动 Electron，不接触用户现有数据；
- 通过真实 preload/IPC 创建 `custom_remote` 占位服务商和连接；
- 服务商和连接在页面正常显示；
- endpoint 未进入页面文本或只读 DTO；
- 默认 `1280×820` 窗口无全局横向溢出；
- 连接筛选与详情标签无横向溢出；
- 能力区在默认窗口下按响应式规则移动到详情区下方；
- 测试完成后临时用户目录已删除。

证据：

```text
docs/active/evidence/phase4/windows/provider-management.png
```

### 本机凭证安全

- Windows `safeStorage.isEncryptionAvailable()` 返回可用；
- Windows 加密/解密往返通过；
- 密文文件不包含测试占位明文；
- renderer 不提供凭证明文读取接口；
- 删除连接只声明本地删除，服务商侧撤销保持 `not_attempted`。

## 三、本次联调修复

- 将模型与服务商三栏转两栏断点从 `1260px` 调整为 `1480px`，计入应用侧栏宽度；
- 默认窗口下能力区移至详情区下方，避免详情标签和右侧能力区过度压缩；
- 连接状态筛选允许换行，不再出现内部横向滚动条；
- 宽屏仍保留与权威设计图一致的三栏结构。

## 四、真实边界与后置项

- 后台尚未提供 Base URL、鉴权、接口路径、请求/响应、错误码、限流和重试契约；
- 当前在线验证、目录同步和能力验证明确返回 `adapter_unavailable`，不伪造成功；
- 未预置服务商、模型、价格、能力、时长、分辨率或数量；
- 费用、隐私和地区保持 `unknown`，路由始终要求提交前确认；
- macOS Keychain 与页面实机验证按计划进入阶段 9；
- 真实 HTTP 适配器必须在后台契约齐备后单独实现和验收。

## 五、结论

阶段 4 本地领域、平台、凭证、页面与 Windows 联调门禁通过，可以正式收口。后台契约缺失作为外部后置阻断继续跟踪，不影响本阶段真实离线和本地管理能力验收。
