# 阶段 3｜联调验收与收口记录

日期：2026-07-22
基线：`develop` / `ca0743b`
验收人：项目负责人兼开发者 B

## 一、阶段结论

阶段 3“全局页面”工程门禁通过，正式收口。

对话、项目、任务中心和作品库已基于阶段 2/3 受控本地契约完成页面接入。阶段 4 及后续创作页面必须继续复用现有项目目录、任务/作品读模型和 `fileId/workId` 文件边界。

## 二、已通过

- 对话页按权威结构实现离线与服务未配置状态，不伪造 AI 回复，不提供直接生成或提交任务入口。
- 项目页作为默认入口，支持真实新建、打开、关闭项目和最近项目状态。
- 项目页展示真实最近任务、最近作品和项目级问题，不制造统计数据。
- 任务中心消费受控全局任务读模型，覆盖真实执行、失败、取消和恢复状态，不显示虚假进度。
- 作品库只消费已登记作品和文件状态，保留缺失、损坏、断盘等异常记录。
- 作品预览与系统文件定位使用短期 `unicomp-media` 句柄和 `workId` 受控操作，不返回绝对路径。
- 四个全局页面在 Windows Electron 可见窗口中完成导航、空白/离线状态和布局联调。
- 深色、浅色主题及离线警告语义测试通过。

## 三、最终门禁

- UI 与安全契约：30 项通过。
- 领域与平台测试：59 项通过。
- 总计：89 项测试通过，0 项失败。
- TypeScript、ESLint、生产构建和 `git diff --check`：通过。
- Vite 生产构建转换 73 个模块。

## 四、Windows 证据

```text
docs/active/evidence/phase3/windows/01-chat.png
docs/active/evidence/phase3/windows/02-projects.png
docs/active/evidence/phase3/windows/03-tasks.png
docs/active/evidence/phase3/windows/04-library.png
```

可见窗口结果：

- 对话页：服务未配置、离线、上下文待检查状态可见；
- 项目页：活动项目、最近项目、创作入口和真实空摘要可见；
- 任务中心：全局筛选和真实空任务状态可见；
- 作品库：筛选和正式作品空状态可见。

## 五、明确后置事项

1. 当前测试项目没有已登记作品，媒体句柄未进行真实大文件可见预览；平台测试已通过，首个真实作品在阶段 5 再复验。
2. 真实 AI 对话等待阶段 4 服务连接和能力契约。
3. macOS 四页实机与自定义协议验证进入阶段 9。
4. 权威图中的在线模型、费用和外发信息不得在阶段 4 契约齐备前伪造。

## 六、负责人决策

项目负责人于 2026-07-22 确认：

- 阶段 3 正式通过并关闭；
- 允许启动阶段 4“模型与服务商”；
- 后续继续从最新 `develop` 创建短期功能分支；
- 未提供的后台契约作为真实阻断项记录，不以猜测实现替代。
