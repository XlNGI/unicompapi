# 阶段 7 B3｜媒体引擎许可与分发决策

日期：2026-07-24
状态：项目负责人已批准选型方向，并批准仅限本地开发/测试的临时例外；正式二进制接入仍等待编解码器与分发审查

## 一、项目许可证

- UniComp 自有代码采用 Apache-2.0；许可证文件为仓库根目录 `LICENSE`。
- `package.json` 的 `license` 字段登记为 `Apache-2.0`。
- 第三方组件不被本项目许可证重新授权，必须按各自许可证和 NOTICE 要求分发。

## 二、媒体引擎选型

项目负责人批准将 FFmpeg 作为 V1.0 的候选媒体引擎，版本固定为 `8.1.2`，同时使用对应版本的 `ffprobe`。

选择理由：

- Windows 与 macOS 具有统一的探测、滤镜、音频混合和导出语义；
- 适合通过 `MediaEngineAdapter` 以独立进程调用；
- 可以在硬件能力失败后建立软件回退尝试；
- 官方提供版本源码、发布签名和许可说明。

## 三、许可与构建边界

- 目标构建为 LGPL-only；不得启用 `--enable-gpl` 或 `--enable-nonfree`；
- `libx264`、`libx265`、`fdk-aac` 等组件不得在未完成独立许可与专利审查前启用；
- 不使用来源、构建参数或许可证无法核验的预编译 npm 媒体包；
- 优先从 FFmpeg 官方签名源码自行构建 Windows/macOS 各架构二进制；
- 发布时必须附带对应源码、构建说明、Hash、许可证和第三方声明。

## 四、工程集成边界

- renderer 不接触媒体引擎路径、命令行、进程句柄或内部日志；
- `MediaEngineAdapter` 只接受结构化 `PreviewPlan`/`ExportPlan`，转换为参数数组或受控库调用；
- 禁止把用户输入、文件名或模型返回内容拼接为任意 shell；
- 必须支持 probe、preview、export、cancel、verifyOutput、异常退出和软件回退测试；
- 未配置真实适配器时继续返回 `adapter_unavailable`，不得显示假进度或假作品。

## 五、尚未批准的项目

以下内容仍需独立审查，完成前不进入正式发布构建：

1. 实际启用的编码器/解码器白名单；
2. H.264、H.265、AAC 等格式的专利与商业分发边界；
3. Windows/macOS 工具链、签名、公证和安装包验证；
4. 每个发布架构的可复现构建、SBOM 和供应链更新策略。

因此，本记录批准的是 FFmpeg 8.1.2 LGPL-only 的工程选型方向和仅限本地开发/测试的项目 `.tools/` 例外；不批准将引擎二进制写入 Git、依赖、生产构建或发布物，也不宣称正式导出已经完成。

## 六、当前工程验证

- 新增开发适配器仅限显式本地开发变量启用，生产构建默认保持不可用；
- 适配器使用参数数组启动外部进程，缓存产物写入临时文件后原子重命名；
- 现有 3 项适配器契约测试继续通过，新增 8 项开发工具链安全测试与 8 项真实媒体引擎契约测试；当前完整回归为 108 项 UI/IPC/工具链契约测试与 185 项领域/平台测试，共 293 项且 0 失败；
- TypeScript 类型检查、ESLint、生产构建和差异检查通过；
- Windows 开发烟测使用 `ffmpeg-n8.1-latest-win64-lgpl-8.1.zip`，下载包 SHA-256 为 `65853acd5274e5d5714e0917d7e9042a24fb6d9d7a4b73efb5a3803e85a0e35c`；
- 二进制报告版本 `n8.1.2-30-g45f1910444-20260723`，构建配置未启用 `--enable-gpl` 或 `--enable-nonfree`，明确禁用 `libx264`、`libx265` 和 `libfdk-aac`，并提供开发预览使用的 `libvpx-vp9` 与 `libopus`；
- 真实进程烟测生成并由 `ffprobe` 复核 WebM VP9/Opus 代理、320x180 JPEG 缩略图和 1200x240 PNG 波形，第二次请求均复用缓存；
- 烟测发现并修正波形输出流映射：`showwavespic` 输出现在显式映射为视频流并禁用音频输出，避免图片容器错误接收原始音轨；
- 项目 `.tools/` 真实安装、重复替换和独立验证通过，Git ignore 规则确认不会跟踪二进制；
- `npm run dev` 已完成 Windows Electron 启动烟测，验证通过后应用保持运行，结束烟测后相关进程已清理；
- `feature/video-media-engine-adapter` 已新增真实 `MediaEngineAdapter`，并通过 Windows `.tools` 的能力、probe、WebM 导出、进度、取消和独立输出校验样本；
- 这些证据只验证当前 Windows 本地开发预览路径，不批准二进制进入 Git、安装包或发布物，也不等同于 B3 正式导出完成。

## 七、临时本地开发例外

项目负责人已明确批准 FFmpeg 仅用于本地开发和测试。该例外必须同时满足：

- 只在 `npm run dev` 的开发进程中启用；
- 安装目录固定在项目 `.tools/media-engine/`，整个 `.tools/` 必须被 Git 忽略；
- 仓库只提交 manifest、安装脚本、验证脚本和测试，不提交压缩包、可执行文件或其他引擎产物；
- 安装前必须匹配 manifest 固定的 SHA-256，解压、删除和切换路径必须受限在 `.tools/`，验证失败时不得替换已有安装；
- `npm run dev` 必须先验证版本、LGPL-only 构建参数、必需编码器和许可证文件，再自动设置 `UNICOMP_ENABLE_LOCAL_FFMPEG=1`、`UNICOMP_FFMPEG_PATH` 与 `UNICOMP_FFPROBE_PATH`；
- 生产启动 `npm start`、生产构建和没有显式开发变量时继续返回 `adapter_unavailable`；
- 只用于受控本地样本的探测、代理预览、缩略图和波形验证，不创建正式 Work；
- 真实发布前仍需重新完成源码、构建、编解码器、专利、签名和分发审查。

开发者示例：

```powershell
npm.cmd run setup:media-engine -- --archive "C:\path\to\ffmpeg-n8.1-latest-win64-lgpl-8.1.zip"
npm.cmd run verify:media-engine
npm.cmd run dev
```

不传 `--archive` 时，安装脚本只可使用 manifest 登记的 HTTPS 地址。该地址当前为上游 rolling `latest` 资产，因此 SHA-256 是强制准入条件：上游文件变化后安装必须失败关闭，不得自动接受新二进制。
