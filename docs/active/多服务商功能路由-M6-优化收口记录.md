# 多服务商功能路由 M6｜优化收口记录

日期：2026-08-04

分支：`feature/provider-routing-optimization-closeout`

来源：`develop@24dcb9b`

证据提交：`254db45`

非快进合并：`a0c75d8`

## 一、范围与停止点

M6 只在包含 M1—M5 全部非快进合并的 `develop@24dcb9b` 上执行最终统一回归、分支保留审计、安全/费用边界复核、可机读证据和计划收口；没有修改业务代码、后台合同、IPC、适配器或页面。

本次不读取或验证真实凭证，不发起真实服务商 HTTP/DNS，不产生收费调用；不执行 macOS 实机与媒体工具链扩展，不启动真实 API 专项或阶段 10。Vidu 图片/视频预算继续封存，Image V1 继续保持未验证。

## 二、统一回归

- `npm.cmd test`：Node 195 项与 Vitest 581 项，共 776 项通过，0 失败、0 跳过；
- `npm.cmd run typecheck`：通过；
- `npm.cmd run lint`：通过；
- `npm.cmd run build`：通过，Vite 89 个模块完成生产构建；
- `npm.cmd run audit:platform`：扫描 291 个文件，0 违规；
- `npm.cmd run verify:handoff`：50 项 checksum、27 个权威资源通过；
- `npm.cmd run verify:recovery-audit`：`passed`，安全违规 0、禁止制品 0；
- `npm.cmd run verify:phase9-closeout`：Windows 九类必需套件保持 `passed`，macOS 保持 deferred；
- `npm.cmd run verify:secure-storage`：Windows x64 可用，明文未持久化；
- `npm.cmd run verify:runtime-integrations`：通过；
- `git diff --check`：通过。

M5 可见验收继续作为最终 UI 证据：深色、浅色与五档窗口覆盖 13 个页面状态，共 130 个组合，无布局/容器溢出、裁切、按钮重叠、零尺寸可用控件或多主操作问题。

Windows 生产 Electron 最终捕获式烟测在禁用 Chromium 后台联网的隔离用户目录中执行：新增 4 个进程、4/4 响应、1 个可见 `UniComp` 窗口，显式 stdout/stderr 捕获为空，正常关闭后残留 0，仓库临时制品 0。

## 三、审计结果

- M1—M5 计划内功能分支共 27 支，本地 27/27、远程 27/27，未删除任何功能分支；
- `develop` 第一父链按顺序包含 M1、本地安全/合同、八个适配器、M4 集成验收和六个 M5 UI 分支；
- 恢复安全审计覆盖 694 个基线跟踪文件，禁止制品 0、公开合同违规 0；
- Git 与生产构建不包含 `.tools/`、FFmpeg/FFprobe 二进制、用户媒体、用户数据或原始运行日志；
- 真实服务商 HTTP 0 次、真实服务商 DNS 0 次、真实凭证读取/验证 0 次、收费调用 0 次、费用 0。

可机读证据与摘要：

- `docs/active/evidence/provider-routing/m6-closeout.json`；
- `docs/active/evidence/provider-routing/m6-closeout-summary.md`。

## 四、已解决执行项

- M5 首轮 Electron 关闭瞬间出现两条 Chromium GPU command-buffer 日志；延长稳定时间后重试为空，且硬件加速瞬时异常未阻断软件导出。
- M6 早期烟测的继承 stderr 在仓库根目录生成进程号临时文件并记录两条 Chromium TLS `net_error -100`；相关文件已精确删除。最终烟测改为禁用后台联网并显式捕获 stdout/stderr，捕获为空、仓库制品为 0。

上述执行项均未涉及真实服务商、凭证或收费，也没有被隐藏、跳过或伪装成首次通过。

## 五、最终结论

多服务商功能路由 M0—M6 在批准边界内全部完成，M6 自验收结论为 `passed`，已通过 `a0c75d8` 非快进合并并推送 `develop`；本地与远程 M6 功能分支继续保留。

合并后停止：阶段 10、真实 API 专项、macOS 实机与媒体工具链扩展均不自动启动。下一步如需继续，必须由项目负责人单独批准阶段 10 或某个真实 API 专项的范围、联网、凭证、费用与验收条件。
