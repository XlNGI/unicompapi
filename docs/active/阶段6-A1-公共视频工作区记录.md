# 阶段 6 A1｜公共视频工作区记录

日期：2026-07-23
分支：`feature/video-workbench-shell`
基线：`38add1e Merge video workspace contracts`

## 一、实际修改

- 快速视频、文生视频、图生视频和基础编辑四个入口继续由 `creationModes.ts` 单一来源驱动；
- 三个视频生成入口复用公共工作区，并接入当前项目及 B1 的 `list/create/update` 本地草稿操作；
- 未打开项目时不能创建视频草稿，保存草稿不会创建或提交任务；
- 展示读取中、无草稿、草稿保存失败、无视频路由、能力未知、素材端口未接入、旧预检过期、适配器不可用和费用未知状态；
- 预览与结果区不显示示例视频、假进度或未登记作品；
- 基础编辑只保留冻结入口和阶段 7 准入说明，不创建编辑草稿、时间线、媒体引擎、代理预览或导出任务。

## 二、真实边界

- 页面不写死服务商、模型、比例、时长、分辨率、帧率、数量、费用或素材槽位；
- 当前只使用项目 Session、视频草稿和只读服务商注册表，不调用上传、预检、任务、执行、远端适配器或结果接收端口；
- 素材选择与预览等待 B2，能力预检与提交等待 B3，正式结果与作品登记等待 B4；
- renderer 继续不接收绝对路径、原始 Hash、凭证、endpoint、远端 operation ID、内部命令或内部错误堆栈。

## 三、自动化与构建门禁

执行：

```text
pnpm.cmd test
pnpm.cmd typecheck
pnpm.cmd lint
pnpm.cmd build
git diff --check
```

结果：

- UI、IPC 与安全契约测试：67 项通过；
- 领域与平台测试：122 项通过；
- 合计：189 项通过，0 失败；
- TypeScript 类型检查通过；
- ESLint 通过；
- Vite 与 Electron TypeScript 生产构建通过；
- Git 差异格式检查通过。

## 四、可见页面检查

在 1080×720 和 1280×720 两种视口下检查快速视频、文生视频、图生视频和基础编辑：

- 四个二级入口、页面标题和真实阻断状态正确；
- 未打开项目时新建与保存按钮禁用；
- 基础编辑明确显示阶段 7 未接入，不出现可操作的假时间线；
- 页面宽度等于视口宽度，无横向溢出；
- 浏览器控制台无警告或错误。

Windows Electron 生产窗口已成功启动，初始项目页可访问状态正常。当前自动捕获工具不能稳定控制该 Electron 窗口，因此真实项目下的新建、保存及四入口点击由项目负责人手工验收。

## 五、未完成项

- A2：快速视频具体输入、单个可选参考素材、轻量确认和派生草稿；
- A3：文生视频上下文、镜头草稿、分镜状态和提示词三层；
- A4：图生视频动态素材槽位、变化要求、增强和提交状态；
- B2、B3、B4 及真实视频生成适配器；
- 阶段 7 基础编辑技术架构与正式实现。
