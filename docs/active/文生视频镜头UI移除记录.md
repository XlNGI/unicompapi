# 文生视频镜头 UI 移除记录

日期：2026-08-21

分支：`feature/remove-text-video-shot-ui`

## 修改范围

- 仅调整文生视频工作台左侧“第一步 · 提交前准备”区域。
- 将首个输入区标题收敛为“视频创意”，保留原始需求、项目上下文与提示词增强链路。
- 删除镜头新增、编辑、删除处理函数以及镜头列表 UI。
- 删除仅由镜头 UI 使用的图标、类型导入与 storyboard 重置辅助函数。
- 模型选择、动态参数、提交面板、自动保存、右侧生成历史和本地作品校验均未修改。
- 所有图片/视频生成双栏工作台统一延伸至底部项目状态栏上方，移除旧高度上限与悬浮状态栏布局遗留的页面空白；卡片内部控件与模型组件不变。
- 删除快速视频与图生视频底部重复的“调用记录”提示卡；任务中心正式调用记录功能与底部项目状态栏保留。
- 领域 DTO 与存量草稿中的 `shots`、`storyboard` 字段继续保留，以兼容已有本地数据；本次不执行破坏性迁移。

## 验证结果

- 文生视频、共享双栏和生成历史定向 Node/UI 合同测试：20 项通过，0 失败、0 跳过。
- 全量 `pnpm.cmd test`：Node/UI/工具链 308 项与 Vitest 745 项，共 1053 项通过，0 失败、0 跳过。
- `pnpm.cmd typecheck`、`pnpm.cmd lint`、`pnpm.cmd build` 与 `git diff --check` 通过。
- 新增合同断言，禁止镜头计划文案、镜头增删控件、镜头列表类名与处理函数回归。
- 实际 React 页面从“视频创作 → 文生视频”进入后，镜头控件零命中、服务与动态能力区域保留、横向溢出为 0，控制台 0 warning/0 error；浏览器环境不连接 Electron 项目数据。
- 未调用真实服务商，未读取凭证，未产生收费请求。

## 未完成项与边界

- 本次不删除领域层兼容字段，也不改写已有文生视频草稿。
- 阶段 10、macOS 实机与媒体工具链延期边界不变。

## 下一步建议

- 后续如需彻底移除 `shots`、`storyboard` 领域字段，应单独立项并提供存量草稿迁移与提交协议兼容方案。
