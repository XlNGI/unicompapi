# UniComp 本地 Agent 宪法

> 适用仓库：`D:/unicompapi-git`
> 适用工具：Codex、Cursor，以及在本工作区执行开发任务的其他 AI Agent。
> 唯一宪法真源：仓库根目录 `AGENTS.md`。
> 本文件是用户个人工作区规则，保持本地未提交、未上传；它不代表远程仓库或其他开发者工作区已经采用相同规则。

## 1. 宪法地位与工具入口

- Codex 和 Cursor 均直接读取根 `AGENTS.md`；不得为它们新增 `.cursor/rules`、`.cursorrules` 或另一份重复宪法。
- 当前仓库未发现 `CLAUDE.md`、`GEMINI.md`、Copilot instructions、Cursor rules、Windsurf、Devin 或 Cline 规则入口。以后确认其他工具时，只允许建立导入、引用或不改变语义的薄摘要，正文仍以本文件为唯一真源。
- 子目录仅在确有独立 owner 且根规则无法清楚表达时才允许新增局部 `AGENTS.md`；它只能收紧局部规则，不得复制、削弱或竞争根真源，新增前必须取得用户确认。
- 项目负责人最新明确决定可以修改产品方向；代码、文档或历史记录不能静默覆盖负责人决定。决定尚未写入项目真源时，先记录证据缺口，不把聊天记忆伪装成仓库事实。
- 本宪法约束工作方式，不替代产品需求、架构记录、阶段计划或验收证据。

## 2. 当前项目事实与阶段边界

- UniComp 是 Electron 桌面 AI 视觉创作工作台，第一版已经发布；当前工作是版本优化、问题修正和功能板块扩展，不是从零立项。
- 当前必需交付平台是 Windows x64。`electron-builder.yml` 当前只声明 NSIS 安装包和目录构建；没有 portable 目标。macOS 是延期目标，未取得实机、打包、签名和媒体工具链证据前不得宣称支持。
- 阶段 9 已在既定边界内收口；阶段 10 尚未启动。签名、公证、自动更新、正式媒体组件分发、SBOM 和正式发布准入仍属于后续阶段。
- 对话内 Office 文档读取与生成能力已经合入 `develop`。`package.json` 与 `src/platform/documents/` 已包含 DOCX、XLSX、PPTX、PDF 相关解析或生成依赖和实现；后续工作是把现有能力安全地 Agent 化并改善业务体验，不得当作尚未实现的空白项目。
- Electron 主进程是受信任的特权执行边界，直接执行受控 Provider 调用；Renderer 只负责展示、输入和有限意图提交，不是后端真源。
- 当前 `BrowserWindow` 已配置 `contextIsolation: true`、`nodeIntegration: false`、`sandbox: false`。前两项是已验证配置事实；sandbox、CSP、导航、外链和远程资源的运行时安全性仍需单独验证。
- Vidu `q3-lite` 图片和 `viduq3-turbo` 视频真实预算已用尽，不得继续真实调用。Image V1 鉴权与 `images` 返回结构仍属于未验证边界。

## 3. 真源优先级

不同事实使用不同 owner，不得用一种真源覆盖另一种真源：

1. 产品范围和可见结果：项目负责人最新明确决定，其次为最终 UI 与开发交接包 V1.2.1，再次为 `PLANS.md` 和 `docs/active/`。
2. 当前可执行实现：当前代码、`package.json`、锁文件、配置、脚本和本轮真实运行证据优先于描述性文档。若实现与文档冲突，先以可执行配置描述现状，再修正文档；不能靠文档声称不存在的 portable、macOS 或发布能力。
3. Agent 行为和工作约束：根 `AGENTS.md` 是唯一真源。
4. 阶段与工程记录：`PLANS.md`、`docs/active/`、`docs/frozen/`。
5. 产品和 UI 权威原件：`handoff/UniComp-技术开发启动包-V1.0.0/03-最终UI交接包-已解压/UniComp-AI-最终UI与开发交接包-V1.2.1/`。只能补工程说明，不得覆盖原件。
6. 历史参考：`docs.rar`、旧代码、旧 UI、归档和旧测试输出。它们不能单独证明当前能力。

若负责人决定、当前实现、权威交接、阶段记录或验收证据互相冲突，且解决方式会改变产品结果、数据、安全、owner 或发布边界，必须停止并提问。

## 4. 必需工作流与推理闸

每个任务开始前必须明确：

1. 当前目标、当前阶段和明确的非目标；
2. 受影响的真源、owner、调用方和数据边界；
3. 被允许修改的文件、禁止触碰的文件和 Git 状态；
4. 成功标准、负面路径、测试决策和验收命令；
5. 是否触及 Provider、凭证、文件系统、用户数据、并发、打包或外部状态。

执行规则：

- 先读最小充分证据，再提出结论或修改。不得凭旧记忆、旧测试数字或通用模板猜测当前项目。
- 暴露所有会改变结果的前提。存在多种需求理解时列明差异和影响，并给出一条推荐主线；只有产品结果、成本、不可逆后果或重大取舍需要用户确认。
- 跨 owner 或架构级变更先形成可验收设计；未获用户批准不得实现。局部修复保持最小范围。
- 架构优先意味着先守住现有 owner、合同和受控执行边界，再选择局部实现；不得用 UI、prompt 或临时适配绕过架构问题。
- 开始前同时写明停止条件；一旦证据冲突、授权不足或风险越过已确认边界，立即停止扩展实现。
- 每一行修改都必须能对应当前目标。不得顺手重构、格式化无关文件、增加投机性抽象或清理历史问题。
- 优先使用项目已有抽象和框架机制；不得新增兼容层、备用链路、通用 Agent 框架或重复 owner 来掩盖当前设计问题。
- 诊断问题必须先复现、收集证据、提出一个可证伪假设，再修改。连续三次修复失败时停止补丁循环，重新审计 owner 和架构边界。
- 默认只读审计不运行 build、test、dev server、install、codegen、migration、deploy、打包或真实 Provider 命令；需要运行时先说明状态影响和验收目的。

## 5. 产品目标、非目标与拒绝方向

### 当前允许的对话页 Agent 能力

- 读取用户明确选择的文档，并在受控范围内做问答、总结、对比和信息提取。
- 生成和编辑 Markdown 草稿。
- 基于结构化大纲生成 PPTX、DOCX、XLSX；PDF 以当前真实实现能力为准，不得把格式名称等同于所有方向均可生成。
- 在对话中展示后端已完成的结构化结果、进度、错误和本地产物状态。
- 复用现有对话、项目上下文、Provider 路由、授权账本、文档解析器和 Office 生成器。

### 明确非目标

- 不恢复“首页”一级入口。
- 不恢复多图参考、图片批量创作或视频批量创作。
- 对话页的办公 Agent 不得变成图片或视频的直接生成入口。
- 不新增登录、会员、充值或云同步，除非项目负责人另行批准。
- 不把 Agent 做成任意命令执行器、任意代码修改器、任意网络爬虫、任意目录扫描器、权限修改器、自动发布器或无人监督自治体。
- 不引入 LangGraph、LangChain Agent、AutoGen、CrewAI、Semantic Kernel 等通用自治运行时作为当前主架构。只有现有受控编排被证明确实无法满足，并经过独立技术选型和用户批准后才可重议。
- 不写死服务商、模型、价格、时长、分辨率或数量；以现有 Provider 包、能力注册和运行配置为准。
- 不为历史 UI、旧文档或未验证平台保留无意义兼容。

## 6. Owner map

| 领域 | 唯一 owner | 宪法约束 |
| --- | --- | --- |
| 产品定位、功能边界、当前阶段 | 项目负责人决定、最终交接包、`PLANS.md`、`docs/active/` | 实现不得从旧代码反推并覆盖产品决定 |
| 领域实体、状态机、不变量、ID | `src/domain/` | UI、IPC 和 Provider 不得另造业务状态真源 |
| 应用用例与业务编排 | `src/application/` | 跨领域流程从用例层编排，不塞入 React 组件或 IPC handler |
| IPC DTO、命名通道、解析合同 | `src/shared/` | Renderer 与主进程只通过显式合同通信，字段默认拒绝 |
| 本地 JSON、Provider、文件、文档解析和 Office 生成运行时 | `src/platform/` | 特权 I/O 和外部调用必须在平台边界受控实现 |
| Electron 组合根和系统能力 | `electron/main.ts`、`electron/preload.ts`、`electron/ipc/` | 主进程决定权限、路径、凭证和执行；preload 只暴露最小 API |
| 对话页和 Agent 交互 | `src/pages/chat/`、`src/components/` | 只拥有交互和展示状态，不拥有 Provider 成功、文件事实或业务状态 |
| Office 文档领域与合同 | `src/domain/entities/document-generation.ts`、`src/shared/document-generation-ipc.ts`、`src/shared/document-attachment-ipc.ts` | 新格式或状态先修改合同和不变量，再改 UI |
| 文档解析和生成 | `src/platform/documents/` | 格式解析、主题、大纲、布局、原子写入和产物校验集中在此，不复制到聊天页 |
| Provider 受控传输和管理 | `src/platform/providers/`、`electron/ipc/provider-ipc.ts` | Agent 不得绕过 Provider registry、授权、预算、传输限制或审计 |
| 本地应用数据路径 | `src/platform/storage/`、`src/platform/repositories/` | 不在 UI 或临时脚本拼接存储路径，不把单次响应当持久化事实 |
| 测试 | `tests/domain/`、`tests/application/`、`tests/platform/`、`tests/ui/` | 测试放在风险所属层，mock 不能证明真实 Provider、Electron 或文件行为 |
| 质量门禁与工程命令 | `package.json`、`scripts/` | 只使用存在的命令，不伪造通过结果 |
| Windows 打包事实 | `electron-builder.yml` | 当前只证明 Windows x64 NSIS/dir 配置，不证明签名、portable 或 macOS |
| Agent 宪法 | 根 `AGENTS.md` | 其他工具入口只能引用或薄摘要 |

当前没有独立 Web 后端、云数据库、登录、租户或付费权益 owner；不得为通用模板完整性而虚构这些模块。以后新增其中任何边界，必须先确定 owner、安全模型、数据迁移和验收。

## 7. 真实 Stack 规则

- 语言和包管理：TypeScript、Node.js、pnpm。保持严格类型和现有导入风格；依赖变更同时检查 `package.json` 与 `pnpm-lock.yaml`，未经批准不换包管理器。
- 桌面运行时：Electron 33。Renderer 永远按不可信前端处理；Node、文件系统、凭证、Provider 和系统能力不得直接暴露给 React。
- 前端：React 18、Vite 5、RSuite 6、`react-markdown`、`remark-gfm`。复用现有布局、组件、样式和交互；不得用整图、截图热区或临时 HTML 冒充生产 UI。
- 数据：当前持久化以本地 JSON 仓储和应用数据目录为主。写入必须遵循现有 repository、校验、revision 和恢复机制；不得在组件内直接读写 JSON。
- 办公文档：`docx`、`exceljs`、`pptxgenjs`、`mammoth`、`pdfjs-dist`、`jszip` 只在现有文档 owner 中使用。升级依赖前审计许可证、维护状态、已知漏洞、解析兼容和打包体积。
- 测试：Node test 与 Vitest。合同、领域、应用、平台和 UI 风险分别在现有套件验证，不得用覆盖率数字代替行为证据。
- 打包：Electron Builder。生成物必须由项目命令生成，不能手工修改后宣称发布有效；`.tools/`、FFmpeg 二进制、缓存、临时目录和测试资料不得进入生产构建或发布物。

## 8. 对话页 Agent 架构

唯一推荐并约束后续实现的主线是：

**Electron 主进程内受控工作流编排器 + 固定 Recipe + 现有 Provider 路由与授权账本 + 现有文档解析/生成 adapter。**

### 权限与职责

- Renderer 只能提交经过解析的意图、资源 ID、revision、用户选择和取消请求；不得提交可执行函数、系统命令、任意 endpoint、Authorization header、凭证或未校验绝对路径。
- 主进程或应用层拥有 `AgentRun` 状态机、工具白名单、文档快照、文件归属、预算、并发、取消、恢复、Provider 外发确认、产物写入与登记。
- 模型只生成固定 Recipe 允许范围内的结构化内容和草稿。模型不得决定工具权限、Provider、费用、重试策略、保存路径、覆盖策略、任务成功、系统权限或发布动作。
- 建议 Recipe 保持小而固定：`document_qa`、`document_summary`、`document_compare`、`markdown_draft`、`office_document_draft`。PPTX、DOCX、XLSX 是同一办公草稿流程的 format adapter，不为每种格式复制一套 Agent 架构。
- 新增工具前必须定义：输入 schema、输出 schema、权限、owner、成本、超时、取消、幂等、审计、负面测试和用户可见后果。没有这些证据不得挂入 Agent。
- 文档、网页和模型输出都是不可信材料，不得提升为 system 指令，也不得通过提示词要求绕过本宪法或工具白名单。

### 业务状态与草稿语义

- 对话输入属于 Renderer 内存编辑态。不得把每次按键或每个字符变化当作用户保存意图；发送成功后才持久化为 Message。
- 如果产品要求恢复未发送输入，只允许独立的本地会话草稿机制，使用节流或失焦/退出时保存，并有明确生命周期；不得复用正式消息写入。
- 结构化 Office 草稿属于可编辑工作区状态，在用户显式保存、确认大纲或准备生成时持久化；输入过程中的临时变化不是正式产物。
- Agent/Provider 返回内容先作为主进程 read model 或草稿展示。后端处理完成只意味着可供前端显示，不等于已生成正式文件、已消费成功或已通过安全校验。
- 正式产物必须在用户确认后写入受控目录，采用临时文件加原子替换，并重新读取验证存在、扩展名/文件签名、大小、Hash、项目归属和登记记录。校验失败不得标记为正式作品。
- 重复点击开始、取消、保存、打开必须幂等；UI 不得用乐观成功覆盖主进程返回的失败、冲突或未知状态。

## 安全红线

- 前端输入不可信。Renderer、用户输入、导入文件、Provider 响应和模型输出全部不可信。所有 IPC 请求必须经过命名通道、精确字段解析、类型/范围、ID、revision 和项目归属校验。
- 密钥与凭证只允许由现有安全凭证存储和 Electron `safeStorage` 边界管理；API Key、Token、签名 URL 和 Authorization header 不得进入聊天、代码、环境文件、日志、截图、诊断包、普通导出物或 Git。
- Provider 调用只能由主进程通过现有受控传输、Provider registry、授权和审计执行。必须限制 endpoint、DNS/IP、方法、header、请求/响应大小、超时、取消、重试和费用；未知结果不得自动重试。
- 文档外发给 Provider 前必须向用户说明文件范围、发送目的、Provider、敏感数据风险和费用，并取得明确确认。默认最小化内容，只发送完成任务所需片段。
- 未经确认不得扩大文件读取范围、对外发送文档、产生费用或启用新的 Agent 工具权限。
- Office/PDF/压缩包解析必须防止路径穿越、压缩炸弹、超大文件、超大解码文本、异常对象数量、恶意关系、公式/外链、宏和解析器资源耗尽。默认拒绝带宏或无法证明安全边界的格式。
- Markdown 渲染禁止原始 HTML、脚本、危险协议、自动远程图片、任意本地文件路径和可执行链接；新增自定义 renderer 时继续保持默认拒绝。
- 日志只记录必要的 run ID、阶段、结构化错误码和脱敏指标；不得记录完整文档、原始 Provider 响应、用户绝对路径或凭证明文。
- 每次代码或设计变更都要自动判断是否触及身份、授权、项目归属、输入信任、凭证、数据写入、第三方、公开暴露或资源上限。命中时必须添加对应负面测试；未命中不强制扩大为全量安全审计。
- 安全审计不等于修复授权。只要求安全审计时保持只读，先报告风险、位置、后果、证据、优先级和修复路线；未经授权不得借审计修改实现。
- 没有本轮真实安全验证证据时不得宣称“安全”“已加固”或“可发布”。静态阅读不能替代 Electron 运行时、恶意文件、真实网络边界和打包产物验证。
- 缺少对应证据的安全边界必须逐项标记为未验证，不得用旧记录或相邻能力推断通过。

## 10. 并发、资源和健壮性

- 同一项目的 JSON 写入必须串行化；同一 `AgentRun` 只能由一个 worker 执行；同一会话默认只能有一个活动文本/Agent 执行。
- Provider 调用由统一资源策略限流。不同项目可受控并行；文件解析和 Hash 只能有界并发，不得无界 `Promise.all`。
- 所有文档数、单文件大小、总解码字符、分块数、输入/输出、Provider 调用数、等待队列、超时、重试和内存占用必须有显式上限。实际数值由一个 `ResourcePolicy` owner 管理，不得散落硬编码。
- 若当前尚无统一策略，首版设计基线为：单次 8 个文档、单文件 2 MiB、总解码 500000 字符、最多 32 块、单 run 最多 8 次 Provider 调用、同项目 1 个活动 Provider、全应用 2 个活动 Provider、同项目解析/Hash 并发 2、同项目等待 5 个、文本输出 2 MiB。实现前必须根据真实文档和 Provider 限额验证并将数值集中到唯一 owner。
- 取消必须贯穿队列、解析、Provider 和生成步骤。已经落盘的正式产物不得因取消被静默删除；临时文件必须可识别并安全清理。
- revision/CAS 冲突必须返回明确错误，不得 last-write-wins 静默覆盖用户数据。
- 应用重启后只恢复可证明幂等的本地步骤。无法确认是否完成的远程调用标记为 `interrupted` 或 `unknown`，等待用户决定，不自动重复付费。
- 错误必须是结构化的 code、stage、retryable、userMessage 和安全 context；不得静默吞错，也不得把内部堆栈、路径或 Provider 原始响应直接显示给用户。

## 测试与验证

- 从领域状态、不变量、IPC 合同和用例开始，再接平台实现与 UI；不得从按钮、prompt 或临时字符串拼接倒推核心合同。
- 实现改动前必须先做测试门禁分类，明确复用的现有门禁、新增的自动化测试或可复现替代证据。
- 稳定可自动化的新行为必须严格执行 RED → GREEN：先让风险所属层的测试因缺少行为而失败，再写最小实现通过。已有因果相关失败门禁时直接复用，不为形式重复测试。
- 纯文档、纯视觉或无法稳定自动化的边界可使用可复现的前后证据，但必须说明为什么不新增自动测试。用户未要求测试或改动很小都不是跳过测试决策的理由。
- 领域状态和 Recipe 合同使用 `tests/domain/`；业务编排使用 `tests/application/`；文件、Provider、仓储和 Electron 边界使用 `tests/platform/`；渲染与页面合同使用 `tests/ui/`。
- mock 只能证明本地分支逻辑，不能证明真实 Provider、凭证存储、Electron 运行时、系统文件选择器、恶意文件防护、Office 文件可打开或打包内容。
- 不得把历史零测试、缺少现成框架或测试编写成本当作跳过回归保护的理由；无法自动化时提供可复现替代证据。
- UI 改动必须验证真实渲染、键盘输入、加载、取消、失败、空状态、重复点击和窗口尺寸；不得只靠源码正则或快照声称用户体验完成。
- Agent/Office 变更至少覆盖：非法 IPC 字段、跨项目 ID、过期 revision、超大/恶意文件、提示注入、外发未确认、Provider 超时/未知、取消竞争、重复保存、同名冲突、生成后 Hash/类型/归属失败和重启恢复。
- 任何完成、通过、修好或发布声明必须附本轮新鲜证据；旧的 566/815 门禁记录只能证明当时基线，不能证明本轮变更。

## 12. 验收命令 map

以下命令均来自当前 `package.json` 或 Git。按风险选择执行，不得机械全跑；默认只列出，未经本轮授权不运行：

| 范围 | 命令 | 当前状态 |
| --- | --- | --- |
| 全量自动化 | `pnpm test` | 本轮未运行 |
| 类型 | `pnpm typecheck` | 本轮未运行 |
| 静态检查 | `pnpm lint` | 本轮未运行 |
| 生产构建 | `pnpm build` | 本轮未运行 |
| 领域 | `pnpm test:domain` | 本轮未运行 |
| 应用编排 | `pnpm test:application` | 本轮未运行 |
| 平台/文档/Provider | `pnpm test:platform` | 本轮未运行 |
| UI 合同 | `pnpm test:ui-contract` | 本轮未运行 |
| 平台假设 | `pnpm audit:platform` | 本轮未运行 |
| 交接一致性 | `pnpm verify:handoff` | 本轮未运行 |
| 恢复审计 | `pnpm verify:recovery-audit` | 本轮未运行 |
| 阶段 9 收口 | `pnpm verify:phase9-closeout` | 本轮未运行 |
| 安全存储 | `pnpm verify:secure-storage` | 本轮未运行 |
| 运行时集成 | `pnpm verify:runtime-integrations` | 本轮未运行 |
| Windows 安装包 | `pnpm package:win` | 高状态影响，未获明确授权不得运行 |
| Windows 目录包 | `pnpm package:win:dir` | 高状态影响，未获明确授权不得运行 |
| 补丁格式 | `git diff --check` | 文档或代码修改后应运行 |

Office Agent 还必须做 Windows Electron 人工验收：选择和解析 PPTX/DOCX/XLSX/PDF、结构化大纲到文件生成、文件可打开、Hash/类型/归属登记、同名冲突、超大或恶意文件拒绝、Provider 外发确认、取消/重启/未知结果、并发重复点击、关闭后进程残留和实际打包内容。没有该证据时只能说自动化或静态检查通过。

## 13. 错误分级

- **阻断问题**：破坏核心功能、owner 边界、安全/隐私、数据真相、构建测试、用户关键链路或当前阶段目标；本轮必须修复或停止。
- **设计风险**：不会立即破坏，但会造成架构漂移、并发失控、资源耗尽或后续难以维护；必须说明取舍、owner 和验收入口。
- **可记录债务**：不影响当前目标链路，可暂缓，但要写明不处理原因、影响和后续入口。
- **无关优化**：不进入本轮，不借任务扩大战线。

不得用情绪化严重程度代替工程优先级；没有证据的担忧不是阻断，有证据的阻断也不能降级成以后再说。

## 14. 文档与交接

- 用户可见行为、IPC 合同、数据语义、架构边界、打包事实或阶段状态变化后，必须更新其现有 owner 文档；不要只改代码，也不要创建第二份竞争真源。
- 内部架构和阶段记录进入 `PLANS.md` 或 `docs/active/` 的既有体系；已完成或失效材料转入 `docs/frozen/` 或明确标记历史。
- 权威交接原件只读；工程补充采用新文件或明确附录，不覆盖原件。
- 每次交付报告：实际修改文件、用户可见结果、运行过的命令和结果、未运行的关键验收、未验证项、风险及下一安全步骤。
- 上下文中断或转交时，必须记录当前目标、已确认边界、Git 状态、修改文件、验证证据、未闭合风险、禁止触碰项和下一条安全命令。

## 15. Git 与本地宪法保留策略

- 不直接在 `main` 开发。业务功能从 `develop` 创建 `feature/*`；大功能拆成可审查的小 PR。除非用户明确改变策略，功能分支合并后保留本地和远程分支用于回溯。
- 不使用 `git add .`；只暂存经用户批准的精确文件。不得提交、推送、创建 PR、发布或部署，除非用户对该动作和范围明确授权。
- 根 `AGENTS.md` 必须保持本地未暂存、未提交、未推送。不得把它加入 `.gitignore`，也不得对它设置 `skip-worktree` 或 `assume-unchanged`，因为这些做法会隐藏上游修改。
- 上游未修改 `AGENTS.md` 时，普通 pull/merge 应保留本地工作树修改；上游修改该文件时，Git 应停止、拒绝覆盖或要求人工解决。不得用 reset、clean、checkout、restore、强制 pull 或脚本静默覆盖本地宪法。
- pull、切换分支、rebase、清理工作树或其他可能触碰该文件的操作前，先检查 `git status --short` 和 `git diff -- AGENTS.md`；如存在覆盖风险，先告知用户并取得备份/处理授权。
- 本地未提交文件无法从远程恢复。工作区、磁盘或机器丢失会使本宪法丢失；需要额外备份时必须由用户指定仓库外的私有位置，不得擅自上传。
- 尊重 dirty worktree。不得回滚、覆盖、吸收或格式化用户未授权的修改；发现无关问题只记录。

## 16. 多 Agent 规则

- 默认单 Agent。只有任务能拆成互不干扰的只读审计或不重叠 owner，并且确实提高速度或证据质量时才使用多 Agent。
- 每个子任务必须有明确范围、输入、禁止事项、预期证据和汇合点；同一文件、同一真源或同一 owner 不得并行写。
- 子 Agent 结论不是最终真源；主 Agent 必须对照当前代码、文档、Git 和测试复核后再交付。

## 17. 必须停止并询问

出现以下任一情况必须停止，给出冲突证据、一条推荐方向、被拒绝方向及原因，并只询问真正改变产品后果的问题：

- 用户决定、当前代码、权威交接、根宪法、阶段记录或验收结果冲突，现有证据不能闭合。
- owner 不清，或实现会产生多个业务状态、Agent 规则、Provider、文件产物或配置真源。
- 需要改变产品定位、一级页面、核心 IPC、领域 schema、权限模型、技术栈或目录架构。
- 需要新增 Agent 工具权限、任意文件访问、目录扫描、系统命令、任意网络、自动发布或无人监督执行。
- 需要真实 Provider、凭证、费用，或将超过已知预算和配额。
- 需要删除、覆盖、迁移用户数据或正式产物，或无法设计可靠回滚。
- 需要改变 Windows/macOS、portable、签名、公证、自动更新、SBOM 或正式发布边界。
- 关键验收不能运行，而缺少它会使完成、安全或发布声明不真实。
- 需要用 Git 操作覆盖、隐藏或丢弃本地未提交的 `AGENTS.md`。

## 18. 完成标准

只有同时满足以下条件，任务才可称为完成：

- 当前目标和非目标均已闭合，修改范围可逐项解释；
- 相关 owner、合同和文档已同步，没有新增竞争真源；
- 正向、负向、失败、取消、并发、数据和安全路径按风险验证；
- 本轮实际命令、结果和用户侧证据已明确列出；
- 未运行或无法验证的边界被逐项标记，没有把计划、mock、静态阅读或旧日志包装成完成；
- Git 范围清楚，未提交、未推送、未部署的状态如实说明。
