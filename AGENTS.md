# UniComp 项目级开发规则

本文件对 Codex 和所有开发者生效。任何实现、重构、文档整理和验收，都必须遵守本规则。

## 一、资料优先级

发生冲突时，按以下顺序处理：

1. 项目负责人最新明确决策；
2. 最终 UI 与开发交接包 V1.2.1；
3. 本文件与 `PLANS.md`；
4. `docs/active/`、`docs/frozen/` 中的正式开发记录；
5. `docs.rar`、旧代码、旧 UI 和历史归档。

最终 UI 与开发交接包位置：

```text
handoff/UniComp-技术开发启动包-V1.0.0/03-最终UI交接包-已解压/UniComp-AI-最终UI与开发交接包-V1.2.1/
```

## 二、严禁事项

- 不得恢复“首页”一级入口。
- 不得恢复多图参考、图片批量创作或视频批量创作。
- 不得把对话页做成直接生成入口。
- 不得加入登录、会员、充值和云同步，除非项目负责人后续明确批准。
- 不得写死服务商、模型、价格、时长、分辨率和数量。
- 不得把 API Key、Token、凭证明文写入代码或提交到仓库。
- 不得在没有本地文件校验时把远端结果标记为正式作品。
- 不得因为硬件加速失败而阻止软件导出。
- 不得跳过 Windows 或 macOS 的跨平台约束。
- 不得在没有真实构建和测试结果时宣称完成。

## 三、工程工作流

- 不直接在 `main` 分支开发。
- 功能开发从 `develop` 创建 `feature/*` 分支。
- 功能分支合并后必须保留本地与远程分支，不得删除，便于按功能回溯和 Debug。
- 每个阶段必须有明确验收结果，失败项记录到 `PLANS.md`。
- 修改交接资料时只能新增工程侧说明，不得覆盖权威原件。
- 大功能应拆成小 PR，避免一次性重写不可审。

## 四、Codex 工作规则

Codex 每次执行任务前应先确认：

1. 当前阶段；
2. 涉及的权威文档；
3. 允许修改的文件范围；
4. 验收方式。

Codex 每次完成任务后应更新：

1. 实际修改内容；
2. 验证结果；
3. 未完成项或阻断项；
4. 下一步建议。

## 五、当前阶段

当前阶段为：

```text
阶段 7｜视频基础编辑
```

阶段 6 已完成联调并正式收口。项目负责人已批准 docs/active/阶段7-技术架构.md 与 docs/active/阶段7-任务拆分.md；B1、B2、A1、A2 与 A3 已完成并合并 develop，A3 已通过项目负责人 Windows Electron 手工验收。项目负责人已批准 FFmpeg 8.1.2 LGPL-only 的选型方向；真实二进制接入仍等待编解码器白名单、专利/商业分发审查和跨平台构建记录，不得提前下载、安装或绑定引擎，硬件失败不得阻止软件导出。
