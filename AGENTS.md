# UniComp 项目级开发规则

本文件对 Codex 和所有开发者生效。任何实现、重构、文档整理和验收，都必须遵守本规则。

## 一、资料优先级

发生冲突时，按以下顺序处理：

1. 项目负责人最新明确决策；
2. 最终 UI 与开发交接包 V1.2.1；
3. 本文件与 `PLANS.md`；
4. `docs/active/`、`docs/frozen/` 中的正式开发记录；
5. `docs.rar`、旧代码、旧 UI 和历史归档。

最终 UI 与开发交接包位置：

```text
handoff/UniComp-技术开发启动包-V1.0.0/03-最终UI交接包-已解压/UniComp-AI-最终UI与开发交接包-V1.2.1/
```

## 二、严禁事项

- 不得恢复“首页”一级入口。
- 不得恢复多图参考、图片批量创作或视频批量创作。
- 不得把对话页做成直接生成入口。
- 不得加入登录、会员、充值和云同步，除非项目负责人后续明确批准。
- 不得写死服务商、模型、价格、时长、分辨率和数量。
- 不得把 API Key、Token、凭证明文写入代码或提交到仓库。
- 不得在没有本地文件校验时把远端结果标记为正式作品。
- 不得因为硬件加速失败而阻止软件导出。
- 不得跳过 Windows 或 macOS 的跨平台约束。
- 不得在没有真实构建和测试结果时宣称完成。

## 三、工程工作流

- 不直接在 `main` 分支开发。
- 功能开发从 `develop` 创建 `feature/*` 分支。
- 功能分支合并后必须保留本地与远程分支，不得删除，便于按功能回溯和 Debug。
- 每个阶段必须有明确验收结果，失败项记录到 `PLANS.md`。
- 修改交接资料时只能新增工程侧说明，不得覆盖权威原件。
- 大功能应拆成小 PR，避免一次性重写不可审。

## 四、Codex 工作规则

Codex 每次执行任务前应先确认：

1. 当前阶段；
2. 涉及的权威文档；
3. 允许修改的文件范围；
4. 验收方式。

Codex 每次完成任务后应更新：

1. 实际修改内容；
2. 验证结果；
3. 未完成项或阻断项；
4. 下一步建议。

## 五、当前阶段

当前阶段为：

```text
阶段 9｜跨平台与完整验收（B1、B2、A1、B3、A2、C1 与 C2 流程 1—6 已合并；C2 流程 7 已完成分支实现；A3、B4、A4 未完成）
```

阶段 8 已完成联调并正式收口，记录见 `docs/active/阶段8-联调验收记录.md`。阶段 9 B1 已通过 `bb862a3` 合并；B2 Windows 实现基线已通过 `f47be6b` 合并；A1 已通过项目负责人 Windows 手工验收并通过 `c7deac1` 合并；B3 已通过 `8b32ba7` 合并；A2 已通过项目负责人 Windows 手工验收并通过 `a6271c3` 合并；C1 对话、项目上下文、受控 IPC 与 UI 接线已完成并通过 `e278b63` 合并，合并记录为 `a6a04a5`。C2 流程 1 已通过 `c03693f`、流程 2 已通过 `7015624`、流程 3 已通过 `3a09d29`、流程 4 已通过 `d629d9c`、流程 5 已通过 `f53b331`、流程 6 已通过 `90a2d58` 非快进合并 `develop`；流程 7 已在 `feature/vidu-e2e-validation` 完成本地合成服务、三协议路由、鉴权与故障矩阵、完整图片 Work 到视频 Work 闭环，并修复图生视频能力目的预检、`remote_completed` 结果接收和截断响应校验。流程 1—7 未使用真实 Vidu Token、未访问真实 Vidu、未产生收费请求。Image V1 鉴权与 `images` 结构仍保持未验证，冻结模型保持 disabled，Evidence 保持 `declared_supported`。A1/A2/B2/B3 的 macOS 实机、B2 Windows 原生目录授权、B3 真实睡眠/锁屏和通知/声音/系统设置人工项仍为 `not_run/deferred`，不得标记阶段跨平台验收完成。C2 详见 `docs/active/阶段9-C2-Vidu官方API接入技术架构.md`、`docs/active/阶段9-C2-Vidu官方API接入任务拆分.md` 与各流程工程记录；流程 8 的真实联网、凭证和收费测试仍需项目负责人再次明确批准。A3、B4、A4 均未自动启动。macOS 本地开发/测试媒体工具链仍须单独批准。阶段 9 不新增业务一级页面；C2 只复用既有页面和业务实体。安装包、代码签名、公证、生产更新、生产媒体组件分发、SBOM 和正式发布准入继续属于阶段 10。`.tools/` 与 FFmpeg 二进制不得进入 Git、生产构建或发布物。
