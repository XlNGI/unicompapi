# UniComp 项目级开发规则

本文件对 Codex 和所有开发者生效。任何实现、重构、文档整理和验收，都必须遵守本规则。

## 一、资料优先级

发生冲突时，按以下顺序处理：

1. 项目负责人最新明确决策；
2. 最终 UI 与开发交接包 V1.2.1；
3. 本文件与 `PLANS.md`；
4. `docs/active/`、`docs/frozen/` 中的正式开发记录；
5. `docs.rar`、旧代码、旧 UI 和历史归档。

最终 UI 与开发交接包位置：

```text
handoff/UniComp-技术开发启动包-V1.0.0/03-最终UI交接包-已解压/UniComp-AI-最终UI与开发交接包-V1.2.1/
```

## 二、严禁事项

- 不得恢复“首页”一级入口。
- 不得恢复多图参考、图片批量创作或视频批量创作。
- 不得把对话页做成直接生成入口。
- 不得加入登录、会员、充值和云同步，除非项目负责人后续明确批准。
- 不得写死服务商、模型、价格、时长、分辨率和数量。
- 不得把 API Key、Token、凭证明文写入代码或提交到仓库。
- 不得在没有本地文件校验时把远端结果标记为正式作品。
- 不得因为硬件加速失败而阻止软件导出。
- 不得跳过 Windows 或 macOS 的跨平台约束。
- 不得在没有真实构建和测试结果时宣称完成。

## 三、工程工作流

- 不直接在 `main` 分支开发。
- 功能开发从 `develop` 创建 `feature/*` 分支。
- 功能分支合并后必须保留本地与远程分支，不得删除，便于按功能回溯和 Debug。
- 每个阶段必须有明确验收结果，失败项记录到 `PLANS.md`。
- 修改交接资料时只能新增工程侧说明，不得覆盖权威原件。
- 大功能应拆成小 PR，避免一次性重写不可审。

## 四、Codex 工作规则

Codex 每次执行任务前应先确认：

1. 当前阶段；
2. 涉及的权威文档；
3. 允许修改的文件范围；
4. 验收方式。

Codex 每次完成任务后应更新：

1. 实际修改内容；
2. 验证结果；
3. 未完成项或阻断项；
4. 下一步建议。

## 五、当前阶段

当前阶段为：

```text
阶段 8｜本地设置（A1、B2、A2、B3 已合并，B4 已完成待合并）
```

阶段 7 已完成联调并正式收口，最终门禁为 307 项测试通过、0 失败、0 跳过；收口记录见 `docs/active/阶段7-联调验收记录.md`。项目负责人已于 2026-07-27 批准阶段 8 技术架构与 A/B 任务拆分。B1 已通过 `1eb3dd8` 合并 `develop`，A1 已通过 `b1bd313 Merge phase 8 A1 local settings shell` 合并 `develop`，B2 已通过 `eb160c9 Merge phase 8 B2 storage performance and media` 合并 `develop`。A2 已在 `feature/local-settings-system-panels` 完成“存储与文件”“任务与性能”“本地媒体处理”三个面板、真实本机状态和两阶段风险确认，通过项目负责人 Windows Electron 手工验收并合并 `develop`；功能分支本地与远程保留。B3 已在 `feature/local-settings-platform-controls` 完成最小授权、真实代理事务与独立加密凭证、通知降级、版本化快捷键和 Windows/macOS 隔离，同步 A2 最新基线后合并 `develop`，功能分支本地与远程保留；独立门禁为 365 项测试通过，A2+B3 集成门禁为 369 项测试通过，类型检查、lint、生产构建和差异检查通过。B4 已在 `feature/local-settings-diagnostics-update` 完成本地日志滚动/分级清理、脱敏诊断包、导入/重置/本机数据清除事务和真实不可用更新状态；集成前门禁为 125 项 Node 与 253 项 Vitest，共 378 项通过，类型检查、lint、生产构建、差异检查和 Windows Electron 烟测通过。B4 尚未合并 `develop`，A3 可继续从当前 `develop` 启动，A4 必须等待 B4 合并。阶段 8 固定实现 10 个分类；普通设置自动保存，目录迁移、代理、并发、硬件加速、清理、重置和清除数据必须先生成影响计划并明确确认。设置只保存在当前设备，平台不支持或未实现能力必须动态隐藏或诚实显示不可用。`.tools/` 与 FFmpeg 仍仅限本地开发/测试，不得进入生产构建或发布物。
