// Shared by the thumbnail producer and renderer; bump the cache version when changing the format.
export const videoEditorThumbnailStripVersion = 'thumbnail-strip-v4';
export const videoEditorThumbnailStripFrameCount = 40;
export const videoEditorThumbnailStripFrameWidth = 160;
export const videoEditorThumbnailStripFrameHeight = 264;
